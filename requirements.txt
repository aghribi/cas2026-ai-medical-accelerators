# Jupyter execution (required by Quarto)
nbclient>=0.9
nbformat>=5.9
ipykernel>=6.29
jupyter>=1.0

# Data / ML
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
scikit-optimize>=0.10
matplotlib>=3.8
plotly>=5.20
pandas>=2.2
tqdm>=4.66
ipywidgets>=8.1

# Heavy deps — only needed locally / in notebooks, not for slide rendering
# Uncomment if running notebooks locally:
# torch>=2.2
# cheetah-accelerator>=0.7
# botorch>=0.11
# gpytorch>=1.12
