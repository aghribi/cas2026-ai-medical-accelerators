---
format:
  revealjs:
    theme: [default, custom.scss]
    slide-number: c/t
    show-slide-number: all
    hash: true
    controls: true
    progress: true
    transition: slide
    background-transition: fade
    highlight-style: github-dark
    code-line-numbers: true
    width: 1280
    height: 720
    margin: 0.05
    center: false
    footer: "CAS 2026 · Jurmala · AI for Medical Accelerators — Accelerator Side · A. Ghribi"
    chalkboard:
      theme: whiteboard
      boardmarker-width: 3
      chalk-width: 3
    mouse-wheel: false
    include-after-body: _afterbody.html
execute:
  echo: true
  eval: false
  warning: false
  message: false
---

```{python}
#| echo: false
#| eval: true
import plotly.io as pio
pio.renderers.default = 'notebook'
```

## {.title-slide background-color="#0d1b2a"}

::: {style="margin-top: 2em;"}
**AI Applications for Medical Accelerators**

::: {style="color: #22d3ee; font-size: 1.3em; font-weight: 700; margin: 0.4em 0;"}
Accelerator Side
:::

CAS Topical Course on Medical Accelerators · Jurmala, Latvia · June 2026

::: {style="color: #94a3b8; font-size: 0.8em; margin-top: 2em;"}
Dr. Adnan Ghribi — GANIL / CNRS-IN2P3  ·  `adnan.ghribi@ganil.fr`
:::
:::

::: notes
Welcome. This lecture covers AI applied to the machine — the accelerator — not the patient. 3 use cases with live notebooks, 10 min of context first. Q&A at the end.
:::

---

## Lecture outline

::: {.col-left}

::: {style="font-size:0.82em; line-height:1.8;"}

**Part 1 &nbsp;—&nbsp; Introduction**

::: {style="margin-left:1.2em; color:#94a3b8;"}
Context &nbsp;·&nbsp; Brief history of AI &nbsp;·&nbsp; AI through the physicist lens
:::

**Part 2 &nbsp;—&nbsp; AI in accelerators**

::: {style="margin-left:1.2em; color:#94a3b8;"}
A 30 years dive &nbsp;·&nbsp; Practical foundations
Practical applications &nbsp;·&nbsp; Future directions & opportunities
:::

::: {style="color:#475569; margin-top:0.6em;"}
Summary &nbsp;·&nbsp; References
:::

:::

::: {style="margin-top: 0.6em; font-size: 0.75em; color: #94a3b8; line-height: 1.8;"}
<a href="https://github.com/aghribi/cas2026-ai-medical-accelerators" style="color:#94a3b8;"><svg class="gh-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>aghribi/cas2026-ai-medical-accelerators</a>
AccML review: [aghribi.github.io/acc-ml-living-review](https://aghribi.github.io/acc-ml-living-review)
:::

:::

::: {.col-right style="padding-top: 0.3em;"}

::: {.fragment .highlight-box style="font-size:0.80em;"}
**Three companion notebooks** — open on Colab now:

- `01_fault_detection` — autoencoder on RF waveforms
- `02_beam_tuning` — Bayesian optimisation with Cheetah
- `03_surrogate_model` — MLP surrogate + ensemble UQ
:::

:::

::: {.clearfix}
:::

::: notes
Time plan — 50 min talk + 10 min Q&A. Hard-stop the talk at 50 to protect questions.
Part 1 (0–10): intro & scope 0–2.5 · what AI is/isn't 2.5–4 · history 4–7 · physicist lens 7–10.
Part 2 (10–47.5): 30-years dive 10–13 · foundations 13–18 · UC1 18–27 · UC2 27–36 · UC3 36–43 · future directions 43–47.5.
Summary 47.5–50 · Q&A 50–60.

Three live notebooks — the code on the slides is the code in those notebooks, all runnable on Colab right now, no installation. A glossary of acronyms is at the very end, just before the backup slides.
:::

---

## Disclaimer — scope & slide guide

::: {.col-left style="font-size:0.88em;"}

**In scope — accelerator side**

::: {style="margin-left:0.5em; line-height:1.9;"}
- RF fault detection
- Beam tuning & daily startup
- Surrogate models for beam physics
- Digital twins
:::

**Out of scope today**

::: {style="margin-left:0.5em; line-height:1.9; color:#94a3b8;"}
- Treatment planning
- Patient-facing clinical AI
- Radiobiology · Regulatory certification
:::

::: {style="font-size:0.62em; color:#64748b; font-style:italic; margin-top:0.45em;"}
→ covered in the companion lecture *"AI — the medical side"*.
:::

:::

::: {.col-right}

<div style="font-size:0.50em; color:#94a3b8; font-weight:700; text-transform:uppercase; letter-spacing:0.10em; margin-bottom:0.5em;">Slide elements you will encounter</div>

<div style="display:flex; flex-direction:column; gap:0.5em;">

<div>
<div style="font-size:0.46em; color:#64748b; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; margin-bottom:0.15em;">① Code block — executable companion notebook</div>
<pre style="background:#111f30; border:1px solid rgba(34,211,238,0.22); border-radius:6px; padding:0.28em 0.7em; font-size:0.58em; margin:0; font-family:'IBM Plex Mono',monospace; color:#67e8f9; white-space:pre;">model.fit(X_train)
anomaly_scores = model.predict(X_test)</pre>
</div>

<div>
<div style="font-size:0.46em; color:#64748b; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; margin-bottom:0.15em;">② Concept box — key physics or ML idea</div>
<div style="background:#140c00; border:2px solid #fbbf24; border-radius:8px; overflow:hidden;">
<div style="background:rgba(251,191,36,0.16); padding:0.2em 0.8em; border-bottom:1px solid rgba(251,191,36,0.38); font-size:0.44em; font-weight:700; color:#fbbf24; letter-spacing:0.10em; text-transform:uppercase;">CONCEPT</div>
<div style="padding:0.2em 0.8em 0.3em; font-size:0.50em; color:#e2e8f0;">Langevin dynamics ≡ stochastic gradient descent</div>
</div>
</div>

<div>
<div style="font-size:0.46em; color:#64748b; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; margin-bottom:0.15em;">③ Article references — bottom of slide</div>
<div style="font-size:0.52em; color:#475569; font-style:italic; border-left:2px solid rgba(71,85,105,0.4); padding-left:0.5em;">Kaiser et al., PRAB 27 (2024) · Duris et al., PRL 124 (2020)</div>
</div>

<div>
<div style="font-size:0.46em; color:#64748b; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; margin-bottom:0.15em;">④ GitHub — slides, notebooks & living review</div>
<div style="font-size:0.60em; color:#22d3ee;">
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" style="width:0.85em;height:0.85em;fill:#22d3ee;vertical-align:-0.12em;margin-right:0.25em;"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>
aghribi/cas2026-ai-medical-accelerators
</div>
</div>

<div>
<div style="font-size:0.46em; color:#64748b; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; margin-bottom:0.15em;">⑤ Colab notebook — open in browser, zero install</div>
<div style="display:flex; align-items:center; gap:0.5em; font-size:0.56em;">
<span style="background:#F9AB00; color:#000; border-radius:3px; padding:0.1em 0.45em; font-weight:700; font-family:'IBM Plex Mono',monospace; white-space:nowrap;">▶ Open in Colab</span>
<span style="color:#94a3b8;">01_fault_detection · 02_beam_tuning · 03_surrogate</span>
</div>
</div>

</div>

::: {style="font-size:0.50em; color:#64748b; font-style:italic; margin-top:0.7em; border-top:1px solid rgba(71,85,105,0.3); padding-top:0.45em;"}
A **glossary** of acronyms is at the end of the deck, before the backup slides.
:::

:::

::: {.clearfix}
:::

::: notes
Orientation slide — two minutes at most. Left: what's in and out of scope. Right: the five visual elements students will see throughout the lecture. After this: Part 1 Introduction.
:::

---

## {.no-footer background-color="#0d1b2a"}

::: {style="margin-top: 3em; text-align: center;"}
::: {style="color: #475569; font-size: 0.6em; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 0.5em;"}
Part 1
:::
::: {style="color: #e2e8f0; font-size: 2.2em; font-weight: 700;"}
Introduction
:::
::: {style="color: #94a3b8; font-size: 0.72em; margin-top: 1em; line-height: 2;"}
Context &nbsp;·&nbsp; Brief history of AI &nbsp;·&nbsp; AI through the physicist lens
:::
:::

::: notes
0–10 min. Four beats: what AI is and isn't (scope the field honestly); a compact history from the McCulloch-Pitts neuron (1943) to the 2024 Nobel Prize in Physics; then the physics–ML bridge (Hopfield as collective behaviour, statistical mechanics, Langevin dynamics). Keep it brisk — the applied payload is Part 2.
:::

---

## What AI is — and isn't

:::: {.columns style="margin-top:0.2em; gap:1.2em;"}
::: {.column width="60%"}

<div style="padding:0.8em; border:2px solid #475569; border-radius:14px; background:rgba(71,85,105,0.06);">
<div style="font-size:0.62em; font-weight:700; color:#94a3b8; text-transform:uppercase; letter-spacing:0.08em;">Artificial Intelligence</div>
<div style="font-size:0.56em; color:#64748b; margin:0.15em 0 0.5em;">symbolic logic · expert systems · search &amp; planning</div>

<div style="padding:0.7em; border:2px solid #60a5fa; border-radius:12px; background:rgba(96,165,250,0.07);">
<div style="font-size:0.60em; font-weight:700; color:#60a5fa; text-transform:uppercase; letter-spacing:0.08em;">Machine Learning</div>
<div style="font-size:0.54em; color:#94a3b8; margin:0.15em 0 0.5em;">classical ML (SVM · RF · kNN) · probabilistic models · <span style="color:#fbbf24;">★ Bayesian opt. / GP</span></div>

<div style="padding:0.6em; border:2px solid #22d3ee; border-radius:10px; background:rgba(34,211,238,0.10);">
<div style="font-size:0.58em; font-weight:700; color:#22d3ee; text-transform:uppercase; letter-spacing:0.08em;">Neural networks · Deep learning</div>
<div style="font-size:0.54em; color:#cbd5e1; margin-top:0.15em; line-height:1.5;">
<span style="color:#fbbf24;">★ autoencoders</span> · <span style="color:#fbbf24;">★ MLP surrogates</span> · CNN / Transformer · deep RL · generative (VAE · diffusion · LLM)
</div>
</div>

<div style="font-size:0.46em; color:#fbbf24; margin-top:0.55em; line-height:1.45;">★ Our <strong>BO/GP</strong> tool lives in this ring — classical ML, <strong>not a neural network</strong>: &ldquo;AI&rdquo; ≠ &ldquo;deep learning&rdquo;.</div>

</div>
</div>

:::
::: {.column width="40%"}

::: {.highlight-box style="font-size:0.74em;"}
**Demystified today — with live code**

- ★ Autoencoder — fault detection
- ★ Bayesian opt. / GP — beam tuning
- ★ MLP surrogate — fast beam physics
:::

::: {style="font-size:0.66em; color:#94a3b8; margin-top:0.6em; line-height:1.6;"}
**Not covered here:** symbolic AI · LLMs &amp; agents · generative models — touched only in *future directions*.
:::

:::
::::

<div style="margin:0.7em auto 0; max-width:90%; background:#140c00; border:2px solid #fbbf24; border-radius:10px; overflow:hidden; text-align:center;">
<div style="background:rgba(251,191,36,0.16); padding:0.18em 0.8em; border-bottom:1px solid rgba(251,191,36,0.38); font-size:0.52em; font-weight:700; color:#fbbf24; letter-spacing:0.14em; text-transform:uppercase;">Key idea</div>
<div style="padding:0.4em 1em 0.5em; font-size:0.74em; color:#e2e8f0;"><strong>Match the method to your data</strong> — how much you have, and whether it is labelled — not to the hype.</div>
</div>

::: {.clearfix}
:::

::: notes
~1.5 min, and the scope-setting slide for the whole lecture. The nested rings are the standard containment view — AI contains machine learning, which contains neural networks and deep learning — but read them as a physicist: each ring is a broader hypothesis class. The point to make explicitly, because it is the common misconception: AI is much wider than deep learning. Symbolic AI, expert systems, and search dominated the field for decades; LLMs and agents are a current frontier we only touch in the futures section. What this lecture builds and demystifies, with runnable code, are the three starred methods — and crucially one of them, Bayesian optimisation with Gaussian processes, is classical probabilistic ML, not a neural network at all. So even our own three use cases span two rings, not one. Everything else on this map is real AI we are deliberately not covering today.
:::

# A Brief History of AI {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.2em; margin-top: 0.6em;"}
From the first neuron to the Nobel Prize in Physics
:::

::: notes
~9 minutes on history. The arc is deliberate: 1943 first mathematical neuron → 2024 Nobel Prize in Physics. The goal is not completeness but to make one point land — this is an 80-year-old, physics-rooted field, and its two "winters" teach healthy scepticism about hype cycles. Keep it brisk; the payoff is the physicist-lens section after.
:::

---

## From neuron to Nobel Prize (1943–2024)

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

events = [
    (1943, "McCulloch-Pitts<br>neuron", 1, "#475569"),
    (1957, "Rosenblatt<br>Perceptron", -1, "#22d3ee"),
    (1969, "Minsky-Papert<br>(XOR limit)", 1, "#f87171"),
    (1982, "Hopfield<br>networks", -1, "#22d3ee"),
    (1986, "Backprop<br>(Rumelhart)", 1, "#22d3ee"),
    (1998, "LeNet<br>(LeCun)", -1, "#a78bfa"),
    (2006, "Deep belief<br>nets (Hinton)", 1, "#a78bfa"),
    (2012, "AlexNet<br>(ImageNet)", -1, "#fb923c"),
    (2017, "Transformers<br>(Attention)", 1, "#fb923c"),
    (2022, "ChatGPT", -1, "#f87171"),
    (2024, "Nobel Prize<br>Physics", 1, "#fbbf24"),
]

fig = go.Figure()
fig.add_shape(type="line", x0=1938, x1=2026, y0=0, y1=0,
    line=dict(color="#22d3ee", width=2.5))

for year, label, side, color in events:
    y_end = 0.52 * side
    fig.add_shape(type="line", x0=year, x1=year, y0=0, y1=y_end * 0.82,
        line=dict(color=color, width=1.5, dash="dot"))
    fig.add_trace(go.Scatter(x=[year], y=[0], mode="markers",
        marker=dict(color=color, size=11, line=dict(color="#0d1b2a", width=2)),
        showlegend=False, hovertext=f"{year}: {label}"))
    fig.add_annotation(x=year, y=y_end,
        text=f"<b>{year}</b><br><span style='font-size:10px'>{label}</span>",
        showarrow=False, font=dict(size=11, color=color), align="center")

fig.update_layout(
    height=380, autosize=True, paper_bgcolor="#0d1b2a", plot_bgcolor="#0d1b2a",
    margin=dict(l=10, r=10, t=10, b=10),
    xaxis=dict(showgrid=False, showticklabels=False, zeroline=False, range=[1936, 2027]),
    yaxis=dict(showgrid=False, showticklabels=False, zeroline=False, range=[-0.9, 0.9]),
    font=dict(color="#e2e8f0"),
)
fig
```

::: notes
The yellow dot at 2024 is the Nobel Prize in Physics awarded to Hopfield and Hinton — formally recognising that the foundations of modern AI are physics.

Note the two "winters" (after 1969 and after the late 1980s) — periods when funding and interest collapsed because the field overpromised. The current period since AlexNet in 2012 has been the longest sustained growth in AI history.
:::

---

## McCulloch & Pitts — the first artificial neuron (1943)

<div style="position:absolute; top:0.2em; right:0.4em; z-index:10;">
<img src="assets/Mccullochpitts.jpeg" style="width:165px; border-radius:6px; border:1px solid rgba(34,211,238,0.4);">
</div>

:::: {.columns style="margin-top:0.2em;"}

::: {.column width="42%"}

**Warren McCulloch** & **Walter Pitts** — first mathematical model of a neuron, mapping neural firing onto Boolean logic (1943).

::: {.fragment style="font-size:0.76em; margin:0.4em 0; line-height:1.6;"}
**How a neuron works:**

- **Dendrites** collect incoming signals from neighbouring neurons
- **Soma** integrates them as a weighted sum $\sum w_i x_i$
- Neuron **fires** if the sum exceeds threshold θ → signal propagates down the **axon**
:::

:::

::: {.column width="56%"}

<img src="assets/bio_neuron_annotated.png"
     style="height:auto; max-width:100%; max-height:31vh; border-radius:4px; display:block; margin:1.0em auto 0.2em auto;">

<img src="assets/mp_artificial_neuron.png"
     style="height:auto; max-width:100%; max-height:30vh; border-radius:4px; display:block; margin:0 auto;">

:::

::::

::: {.slide-ref}
McCulloch & Pitts (1943). A logical calculus of the ideas immanent in nervous activity. *Bull. Math. Biophysics* 5, 115–133.
:::

::: notes
The starting point. McCulloch (a neurophysiologist) and Pitts (a logician) showed a neuron can be modelled as a threshold logic gate: take a weighted sum of inputs and fire if it exceeds a threshold θ. Two things to stress. First, the biological-to-mathematical mapping — dendrites are the inputs, the soma performs the weighted summation, the axon carries the output. Second, and crucially, the weights here are fixed by hand: there is no learning yet. Learning is what Rosenblatt adds on the next slide. So this is the architecture of a neuron without the ability to adapt.
:::

---

## The perceptron — first learning algorithm (1957)

::: {.col-left}

<img src="assets/portrait_rosenblatt.png" style="float:right; margin: 0 0 0.4em 0.8em; border-radius:6px; border:1px solid rgba(34,211,238,0.4); width:110px;">

**Frank Rosenblatt, 1957** — the Perceptron:

$$y = \text{sign}\!\left(\sum_i w_i x_i - \theta\right)$$

One neuron: $N$ weighted inputs $x_i$, one binary output $y$.
Unlike McCulloch-Pitts, the weights $w_i$ are **not fixed** — they are **learned** from labelled examples via an error-correction rule.

:::

::: {.col-right}

::: {.highlight-box}
**The perceptron learning rule:**

$$w_i \leftarrow w_i + \eta \cdot (y - \hat{y}) \cdot x_i$$

Every modern network still follows this principle.
:::

::: {.key-result style="margin-top: 0.8em;"}
Rosenblatt's Mark 1 Perceptron (1958) was a physical hardware device — it made the front page of the New York Times.
:::

:::

::: {.clearfix}
:::

::: {.slide-ref}
Rosenblatt, F. (1958). The perceptron: a probabilistic model for information storage and organization in the brain. *Psychol. Rev.* 65(6), 386–408.
:::

::: notes
**Is the perceptron "a single neuron"?** Yes — exactly one neuron. The formula shows it: N inputs x_i each with a weight w_i, summed and passed through a sign function to give one binary output y. It is not a network; it is a threshold logic unit, just like McCulloch-Pitts. The key difference from M-P is what Rosenblatt added: the weights are not programmed by hand — they are adjusted automatically each time the neuron makes a mistake.

**Relationship to Hebbian learning:** Hebb (1949) proposed a biological rule — "neurons that fire together wire together": w_ij increases when neurons i and j are simultaneously active. Rosenblatt was conceptually inspired by this, but his update rule fires on ERROR, not on co-activation. Formally: Δw_i = η·(y − ŷ)·x_i. If the output is correct (y = ŷ), nothing changes. Only a mismatch drives an update. This is error-correction, not Hebbian.

**Distinction from backpropagation (1986):** The perceptron uses a sign (step) activation — non-differentiable, so no gradient can propagate. Backprop requires smooth, differentiable activations (sigmoid, ReLU). The perceptron also has no hidden layers. Modern deep networks are descendants conceptually but not mathematically.

The Mark 1 Perceptron at Cornell learned to recognise letters from photographs. New York Times (1958): "New Navy Device Learns By Doing." It was a physical machine wired with potentiometers as weights.
:::

---

## AI winters and the deep learning renaissance

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

events = [
    (1943, "McCulloch\n& Pitts", "#475569", 10),
    (1957, "Perceptron", "#22d3ee", 12),
    (1969, "1st Winter\n(XOR)", "#f87171", 14),
    (1974, "Backprop\n(Werbos)", "#94a3b8", 10),
    (1986, "Backprop\npopularised", "#22d3ee", 14),
    (1995, "2nd Winter\n(SVMs win)", "#f87171", 14),
    (2006, "Deep Belief\nNets", "#94a3b8", 10),
    (2012, "AlexNet\n★", "#fb923c", 18),
    (2017, "Transformer", "#a78bfa", 14),
    (2023, "GPT-4\nLLMs", "#fb923c", 16),
]

fig = go.Figure()

# Winter shading
for x0, x1 in [(1969, 1985), (1995, 2005)]:
    fig.add_shape(type="rect", x0=x0, x1=x1, y0=0.3, y1=0.7,
                  fillcolor="rgba(248,113,113,0.07)",
                  line=dict(color="rgba(248,113,113,0.2)", width=1, dash="dot"))

fig.add_annotation(x=1977, y=0.73, text="1st winter",
    font=dict(size=11, color="#f87171"), showarrow=False)
fig.add_annotation(x=2000, y=0.73, text="2nd winter",
    font=dict(size=11, color="#f87171"), showarrow=False)

# Timeline baseline
fig.add_shape(type="line", x0=1940, x1=2026, y0=0.5, y1=0.5,
              line=dict(color="#1e3a5a", width=2))

for year, label, color, size in events:
    fig.add_trace(go.Scatter(
        x=[year], y=[0.5],
        mode="markers+text",
        marker=dict(size=size, color=color, opacity=0.9,
                    line=dict(color="rgba(255,255,255,0.2)", width=1)),
        text=[label],
        textposition="top center" if year not in (1969, 1995) else "bottom center",
        textfont=dict(size=11, color=color if color != "#475569" else "#64748b"),
        showlegend=False,
    ))

fig.update_layout(
    height=330,
    autosize=True,
    paper_bgcolor="#0d1b2a", plot_bgcolor="#0d1b2a",
    xaxis=dict(range=[1938, 2026], showgrid=False,
               tickvals=[1943,1957,1969,1986,1995,2012,2017,2023],
               ticktext=["1943","1957","1969","1986","1995","2012","2017","2023"],
               tickfont=dict(size=10, color="#64748b"),
               tickangle=-45),
    yaxis=dict(visible=False, range=[0.1, 1.0]),
    margin=dict(l=5, r=5, t=25, b=55),
    showlegend=False,
    title=dict(text="70 years from threshold logic to large language models",
               font=dict(size=10, color="#475569"), x=0.5),
)
fig
```

:::: {.columns style="margin-top:0.4em; gap:0.8em;"}
::: {.column width="33%"}
::: {.fragment .highlight-box style="font-size:0.76em;"}
**1969 — First winter**
Minsky & Papert prove perceptron cannot solve XOR.
:::
:::
::: {.column width="33%"}
::: {.fragment .highlight-box style="font-size:0.76em;"}
**1986 — Backpropagation**
Gradients through layers — the key mathematical unlock.
:::
:::
::: {.column width="33%"}
::: {.fragment .highlight-box style="font-size:0.76em;"}
**2012 — AlexNet** cuts ImageNet error by 10 pp.
Same idea, scaled. Deep learning era begins.
:::
:::
::::

::: notes
The Minsky-Papert result was a setback — XOR requires a hidden layer, which the perceptron lacked. The real fix was the sigmoid: a smooth, differentiable activation that lets gradients flow backward through multiple layers. A step function has zero gradient almost everywhere. Backprop was actually derived by Werbos in 1974 but ignored; Rumelhart, Hinton & Williams popularised it in 1986. The second winter was driven by SVMs and kernel methods outperforming neural nets on small datasets. AlexNet won on GPUs — the compute unlock, not a conceptual breakthrough.
:::

# AI Through the Physicist Lens {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
Energy, spins, and statistical mechanics
:::

::: {style="font-size: 1em; color: #334155;"}
Hopfield networks · Boltzmann machines · the physics–ML bridge
:::

::: notes
~8–12 min. Hopfield networks as spin glass systems, the Ising connection, statistical mechanics of learning, SGD as Langevin dynamics. The point: accelerator physicists are not outsiders in ML — the toolkit is the same. Keep brisk.
:::

---

## Physics at the core of AI

<div style="position:absolute; top:0.2em; right:0.4em; z-index:10; display:flex; gap:0.7em;">
<figure style="display:flex; flex-direction:row; align-items:center; gap:0.3em; margin:0;">
<figcaption style="font-size:0.55em; color:#94a3b8; text-align:right; white-space:nowrap;">J. Hopfield</figcaption>
<img src="assets/portrait_hopfield.png" style="border-radius:6px; border:1px solid rgba(34,211,238,0.4); height:115px;">
</figure>
<figure style="display:flex; flex-direction:row; align-items:center; gap:0.3em; margin:0;">
<figcaption style="font-size:0.55em; color:#94a3b8; text-align:right; white-space:nowrap;">G. Hinton</figcaption>
<img src="assets/portrait_hinton.png" style="border-radius:6px; border:1px solid rgba(34,211,238,0.4); height:115px;">
</figure>
</div>

::: {.col-left}

**John Hopfield, 1982** — stores memories as energy minima of the Ising Hamiltonian:

$$E = -\frac{1}{2} \sum_{ij} w_{ij} s_i s_j$$

**Geoffrey Hinton** extended this with the Boltzmann distribution $P(s) \propto e^{-E/T}$ — giving neural networks a thermodynamic learning rule.

:::

::: {.col-right}

::: {.highlight-box style="margin-top:2.5em;"}
**Nobel Prize in Physics 2024**

*"for foundational discoveries enabling machine learning with artificial neural networks"*

— **John J. Hopfield** & **Geoffrey E. Hinton**

Awarded in **Physics**, not Computer Science.
:::

:::

::: {.clearfix}
:::

::: notes
The Nobel Committee's citation is explicit: Hopfield's use of statistical mechanics to build associative memory, and Hinton's extension to probabilistic models using the Boltzmann distribution, are the foundations.

As accelerator physicists, we think in terms of Hamiltonians, phase spaces, and energy minimisation every day. ML is a natural extension of our toolkit.
:::

---

## Hopfield networks — neurons as spins

:::: {.columns style="margin-top:0.2em;"}
::: {.column width="55%"}

::: {style="font-size:0.72em; color:#22d3ee; border-left:3px solid rgba(34,211,238,0.5); padding:0.2em 0.6em; margin-bottom:0.4em;"}
**A collective-effects view:** a beam's coherent modes emerge from *pairwise particle interactions*; a Hopfield memory emerges from *pairwise neuron couplings*. Both are many-body systems governed by an energy function.
:::

The same mathematics describes **Ising spins** and **neural networks**:

::: {style="font-size:0.80em; line-height:1.5;"}
| Ising model | Hopfield network |
|:---|:---|
| Spin $s_i \in \{-1,+1\}$ | Neuron state |
| Coupling $J_{ij}$ | Synaptic weight $w_{ij}$ |
| $E = -\tfrac{1}{2}\sum J_{ij}s_is_j$ | Same Hamiltonian |
| Ground state | Stored memory |
:::

:::
::: {.column width="43%"}

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go
from plotly.subplots import make_subplots

np.random.seed(7)
N = 10
p = -np.ones((N, N))
p[:, 1] = 1; p[:, 8] = 1       # vertical bars of "H"
p[4:6, 1:9] = 1                 # crossbar

noisy = p.copy()
noisy[np.random.rand(N, N) < 0.28] *= -1

fig = make_subplots(rows=1, cols=2,
    subplot_titles=["Stored memory ξ", "Noisy input (28 % flipped)"],
    horizontal_spacing=0.10)
cscale = [[0, "#f87171"], [0.5, "#0d1b2a"], [1, "#22d3ee"]]
for col, grid in enumerate([p, noisy], 1):
    fig.add_trace(go.Heatmap(
        z=grid, zmin=-1, zmax=1, colorscale=cscale, showscale=False,
    ), row=1, col=col)
    fig.update_xaxes(showticklabels=False, showgrid=False, zeroline=False, row=1, col=col)
    fig.update_yaxes(showticklabels=False, showgrid=False, zeroline=False, row=1, col=col)
fig.update_layout(
    height=310, autosize=True,
    paper_bgcolor="#0d1b2a", plot_bgcolor="#0d1b2a",
    font=dict(color="#e2e8f0", size=11),
    margin=dict(l=5, r=5, t=28, b=5),
)
fig
```

::: {style="font-size:0.70em; color:#94a3b8; text-align:center; margin-top:0.15em;"}
Red = −1 (spin ↓) · Cyan = +1 (spin ↑)
:::

::: {style="margin-top:0.5em; font-size:0.72em; border-top:1px solid rgba(148,163,184,0.2); padding-top:0.4em;"}
**Hebbian learning** — to store pattern $\xi^\mu$:
$$w_{ij} = \frac{1}{N}\sum_\mu \xi_i^\mu \xi_j^\mu$$
:::

:::
::::

::: {.slide-ref}
Hopfield, J.J. (1982). Neural networks and physical systems with emergent collective computational abilities. *PNAS* 79, 2554–2558.
:::

::: notes
**Lead with the collective-effects framing (this is the hook for our audience):** as accelerator physicists we never reason about one particle — we reason about the *collective* behaviour of the whole beam, where coherent modes, instabilities, and space-charge effects emerge from the pairwise interactions between particles, and the whole thing is governed by a Hamiltonian. A Hopfield network is the same kind of object: many simple units (neurons/spins), pairwise couplings, and a global energy whose minima are the interesting states. The difference is only what the minima *mean* — for a magnet, an ordered phase; for the network, a stored memory. So this is not a borrowed metaphor; it is the same many-body bookkeeping we already do for beams, applied to computation.

**The table — Ising model and Hopfield network side by side:**

The Ising model (Lenz 1920, Ising 1925) was invented to describe ferromagnetism. Every iron atom carries a magnetic moment (a spin) that can point up (+1) or down (−1). Neighbouring atoms interact: when both spins are aligned the energy is lower, so the system tends towards alignment. The total energy is E = −½ Σ_{ij} J_{ij} s_i s_j. In equilibrium the system sits at the minimum-energy configuration — all spins aligned (ferromagnet).

Hopfield's 1982 insight: re-read those variables as a neural network:
- Spin s_i ∈ {−1,+1} → neuron i is silent (−1) or active (+1).
- Coupling J_{ij} between atoms i and j → synaptic weight w_{ij} between neurons i and j.
- Energy minimum of the magnet → a stored memory pattern.

This is not an analogy — it is the same equation. A Hopfield network IS a physical spin system. The mathematics of ferromagnetism already contained the mathematics of associative memory; Hopfield made that explicit.

**The Hebbian weight matrix — w_{ij} = (1/N) Σ_μ ξ_i^μ ξ_j^μ:**

To store P patterns ξ^1, ξ^2, ..., ξ^P (each a binary vector of ±1), you set the weight between neurons i and j to the average of their products over all stored patterns. If neurons i and j tend to be in the same state (both +1 or both −1) across many patterns, their weight is large and positive — exactly Hebb's rule. Each pattern contributes one rank-1 outer-product matrix to W; the total weight matrix is their sum (normalised by N).

**The figure — two heatmaps:**
- Left: the letter "H" encoded as a 10×10 grid. Cyan pixels = +1 (spin ↑), red pixels = −1 (spin ↓). This is the stored memory ξ.
- Right: the same pattern with 28% of pixels randomly flipped. This is a corrupted or partial input given to the network at retrieval time. Despite 28% noise, the network converges back to the clean "H". That is content-addressable memory: query with a fragment, retrieve the whole.
:::

---

## Statistical mechanics → deep learning

::: {style="margin-top:0.15em; font-size:0.72em;"}

| Physics concept | ML analogue |
|:---|:---|
| **Hamiltonian / energy** $H(s)$ | **Loss function** $\mathcal{L}(\theta)$ |
| **Partition function** $Z = \sum_s e^{-H/T}$ | Normalisation constant (softmax, VAE) |
| **Temperature** $T$ | Learning rate / annealing schedule |
| **Boltzmann distribution** $P \propto e^{-E/T}$ | Softmax output — Boltzmann machine |
| **Phase transition** | Grokking (sharp generalisation threshold) |
| **Langevin dynamics** $\dot{x} = -\nabla E + \eta$ | **Stochastic gradient descent (SGD)** |
| **Free energy** $F = -T\ln Z$ | ELBO / variational lower bound (VAE) |

:::

::: {.key-result style="margin-top:0.4em; font-size:0.82em;"}
Not metaphors — the **same mathematics**, rediscovered independently.
As accelerator physicists we already speak this language.
:::

::: notes
**Spend ~10 s here — point, don't read.** One sentence: every column is a physics concept the audience already owns, re-labelled for ML — the cleanest being "SGD ≈ Langevin dynamics." Then move on; come back to any row only if asked.

Reference detail for questions: the SGD–Langevin connection was formalised by Welling & Teh (2011). The Boltzmann machine (Ackley, Hinton & Sejnowski 1985) is a literal physical model. Phase transitions in learning (grokking) are an active topic since Power et al. (2022). The deeper Hopfield capacity/retrieval material is now in the backup section if a question goes there.
:::

## {.no-footer background-color="#0d1b2a"}

::: {style="margin-top: 3em; text-align: center;"}
::: {style="color: #475569; font-size: 0.6em; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 0.5em;"}
Part 2
:::
::: {style="color: #e2e8f0; font-size: 2.2em; font-weight: 700;"}
AI in accelerators
:::
::: {style="color: #94a3b8; font-size: 0.7em; margin-top: 1em; line-height: 2.2;"}
A 30 years dive &nbsp;·&nbsp; Practical foundations
Practical applications &nbsp;·&nbsp; Future directions & opportunities
:::
:::

::: notes
12–57 min. The core of the lecture: (1) a 30-year milestone dive through ML at accelerators; (2) the six AI application categories and the ML pipeline; (3) three hands-on use cases with live Python notebooks; (4) digital twins; (5) open problems and outlook.
:::

# A 30 Years Dive in AI for Accelerators {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
From research curiosity to operational tool
:::

::: {style="font-size: 1em; color: #334155;"}
Milestones · concrete results · living review
:::

::: notes
~3 minutes. The message: AI at accelerators is not new — the first neural-network control work was 1992–94. Then two largely dormant decades, limited by compute, before the field reignited around 2016. End on the KARA −30% energy result, because it is measured on a real ring, not a simulation — proof that this is operational, not just a research curiosity.
:::

---

## 30 years of AI at accelerators

<style>
.ai-milestones table { width: auto !important; margin: 0 auto !important; min-width: 88%; }
.ai-milestones td, .ai-milestones th { padding: 0.45em 1.1em !important; }
</style>

::: {.ai-milestones style="font-size: 0.66em; margin-top: 0.4em;"}

| Year | Facility | Approach | Impact |
|------|----------|----------|--------|
| 1992–94 | Ion source · storage rings | Adaptive NN control (Mead+92); NN orbit correction (Bozoki & Friedman 94) | First ML for accelerator control |
| 2020 | SLAC LCLS | Bayesian optimisation in production | setup hours → ~tens of evaluations |
| 2020 | JLab CEBAF | ML on SRF cavity waveforms | Cavity-fault classification |
| 2024 | DESY / KIT | Cheetah differentiable simulator | 10³–10⁵× faster than tracking codes |
| 2020–23 | CERN · DESY | Reinforcement learning for control & tuning | Autonomous closed-loop control |
| 2024 | KIT KARA | Anomaly detection + energy management | **−30% energy footprint** on real ring |

:::

::: {.slide-ref}
Edelen et al., *IEEE TNS* 63 (2016) · Duris et al., *PRL* 124 (2020) · Kaiser et al., *PRAB* 27 (2024) · Ghribi et al., *EPN* 56 (2025)
:::

::: notes
30 years from first NNs to production AI systems. The earliest applications are Mead et al. (1992, adaptive NN control of an ion source) and Bozoki & Friedman (1994, NN orbit correction in storage rings) — then two dormant decades limited by compute, before project funding revived the field around 2016 (Edelen+2016). By 2020, BO was in production at LCLS (Duris+2020), with ML-based model-independent stabilisation demonstrated at ALS (Leemann+2019, PRL 123:194801) and differentiable simulation via Cheetah (Kaiser+2024, PRAB). Reinforcement learning entered control with sample-efficient RL at CERN (Kain+2020, PRAB 23:124801) and online RL+BO tuning at DESY (Kaiser+2023). KARA result is not a simulation — measured on the real machine.
:::

---

## AI in accelerators — concrete results

:::: {.columns}

::: {.column width="40%"}

::: {.fragment .fade-in-then-semi-out style="margin: 0.7em 0; font-size:0.92em;"}
**Fault detection** — production ML triage
:::

::: {.fragment .fade-in-then-semi-out style="margin: 0.7em 0; font-size:0.92em;"}
**Beam tuning** — BO + RL, closed-loop
:::

::: {.fragment .fade-in-then-semi-out style="margin: 0.7em 0; font-size:0.92em;"}
**Simulation** — differentiable, fast surrogates
:::

::: {.fragment .fade-in-then-semi-out style="margin: 0.7em 0; font-size:0.92em;"}
**Optimisation toolboxes** — facility-agnostic
:::

:::

::: {.column width="60%"}

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

labels  = ['Badger + Geoff<br>opt. toolboxes',
           'KARA<br>energy AI', 'Cheetah<br>simulation',
           'LCLS<br>beam setup', 'CEBAF<br>fault detection']
metrics = ['multi-facility', '−30% energy', '10⁵× faster', '~50 evaluations', 'production ML triage']
values  = [0.88, 0.72, 1.0, 0.86, 0.95]
colors  = ['rgba(34,211,238,0.55)'] * 5

fig = go.Figure(go.Bar(
    y=labels, x=values,
    orientation='h',
    marker=dict(color=colors, line=dict(color='#22d3ee', width=1.2)),
    text=metrics, textposition='outside',
    textfont=dict(color='#22d3ee', size=12),
    width=0.52,
))
fig.update_layout(
    height=420, autosize=True,
    paper_bgcolor='#0d1b2a', plot_bgcolor='#111f30',
    font=dict(color='#e2e8f0', size=11),
    xaxis=dict(showgrid=False, showticklabels=False, zeroline=False, range=[0, 1.65]),
    yaxis=dict(showgrid=False, tickfont=dict(size=11)),
    margin=dict(l=110, r=130, t=20, b=10),
    showlegend=False,
)
fig
```

:::

::::

::: {.slide-ref}
Tennant et al., *PRAB* 23 (2020) · Duris et al., *PRL* 124 (2020) · Kaiser et al., *PRAB* 27 (2024) · Ghribi et al., *EPN* 56 (2025) · Zhang et al., *IPAC* 2022 (Badger)
:::

::: notes
These are production results, not simulations. CEBAF (JLab) has run ML-based SRF cavity-fault classification in production since ~2019 (Tennant et al. 2020 is a supervised classifier study; the unsupervised autoencoder in the live-demo notebook is our own illustration, not the CEBAF system). LCLS BO is the Duris+2020 PRL paper — real shots. KARA is the Ghribi+2025 EPN result with measured ring energy reduction. Badger (SLAC) and Geoff are open-source optimisation plugin frameworks that let operators swap algorithms (BO, Nelder-Mead, RL) without rewriting beamline code; both are in active production use at multiple facilities.
:::

---

## {.no-footer background-iframe="https://aghribi.github.io/acc-ml-living-review/" background-interactive="true"}

::: notes
This living review covers all ML applications in accelerator physics — updated continuously. It is the map of what has been done and what is open. Bookmark it.
:::

---

## Five ways AI helps accelerators today

:::: {.columns}

::: {.column width="40%"}
::: {style="font-size:0.78em; line-height:1.6;"}

::: {.fragment .fade-in-then-semi-out #box-fault}
- **Fault detection** — classify anomalous events before interlocks trigger
:::

::: {.fragment .fade-in-then-semi-out #box-tuning}
- **Beam tuning &amp; adaptive control** — optimal settings in < 50 evaluations; online RL in the loop
:::

::: {.fragment .fade-in-then-semi-out #box-surrogate}
- **Surrogate models** — replace slow tracking codes (10³–10⁵× speedup)
:::

::: {.fragment .fade-in-then-semi-out}
- **Predictive maintenance** — forecast failures before they occur
:::

::: {.fragment .fade-in-then-semi-out}
- **Digital twins** — live virtual model of the actual machine, continuously updated
:::

<span class="fragment" id="lecture-focus-trigger" style="display:inline-block;width:0;height:0;overflow:hidden;"></span>

:::
:::

::: {.column width="60%"}

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

# Bottom-to-top: Digital twins (bar 0, least mature) → Fault detection (bar 5, most mature)
# Fragment index matches bar index: fragment 0 → bar 0, ..., fragment 5 → bar 5
ways      = ['Digital twins', 'Pred.<br>maintenance', 'Surrogate<br>models',
             'Beam<br>tuning', 'Fault<br>detection']
readiness = [3.5, 4.2, 6.2, 7.2, 7.8]
# All bars start invisible — JS state machine drives colors
colors    = ['rgba(0,0,0,0)'] * 5

fig = go.Figure(go.Bar(
    y=ways, x=readiness,
    orientation='h',
    marker=dict(color=colors, line=dict(color='rgba(255,255,255,0.08)', width=0.5)),
    width=0.60,
))
fig.add_shape(type='line', x0=5.5, x1=5.5, y0=-0.5, y1=4.5,
    line=dict(color='rgba(34,211,238,0.35)', width=1.2, dash='dot'))
fig.add_annotation(x=5.5, y=4.8, text='in production →',
    showarrow=False, font=dict(size=9, color='#22d3ee'), xanchor='left')

fig.update_layout(
    height=560, autosize=True,
    paper_bgcolor='#0d1b2a', plot_bgcolor='#111f30',
    font=dict(color='#e2e8f0', size=10),
    xaxis=dict(
        title='Deployment readiness',
        range=[0, 9.5],
        tickvals=[2, 4, 6, 8],
        ticktext=['concept', 'validated', 'production', 'mature'],
        showgrid=True, gridcolor='#1e3048', zeroline=False,
    ),
    yaxis=dict(showgrid=False, tickfont=dict(size=10.5)),
    margin=dict(l=90, r=20, t=15, b=55),
    showlegend=False,
)
fig
```

:::

::::

::: notes
This lecture covers the first three in depth with runnable code.
:::

# Practical Foundations {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
From raw signals to trained model
:::

::: {style="font-size: 1em; color: #334155;"}
Data · Features · Method selection · Pipeline
:::

::: notes
~7 minutes. Before the use cases, the unglamorous but decisive part: data quality, feature engineering, method selection, and the deploy pipeline. The theme to repeat throughout: in operational accelerator ML the bottleneck is almost never the model — it is the data and the integration into the control system.
:::

---

## The ML pipeline — from data to inference {data-auto-animate=true}

<div data-id="pipe-row" style="display:flex; align-items:stretch; gap:0.22em; margin-top:6em; width:100%;">
<div data-id="p1" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Raw Data</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Waveforms · images<br>logs · set-points</div>
</div>
<div data-id="a1" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p2" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Pre-process</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Clean · sync<br>normalise · resample</div>
</div>
<div data-id="a2" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p3" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Feature extract</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">FFT · Gaussian fit<br>statistics · domain</div>
</div>
<div data-id="a3" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p4" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Train &amp; validate</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">80/20 split<br>cross-val · AUC</div>
</div>
<div data-id="a4" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p5" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Deploy &amp; infer</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Sub-ms response<br>monitoring · retrain</div>
</div>
</div>

<div data-id="uc-row" style="display:flex; gap:0.35em; margin-top:2em; width:100%;">
<div data-id="uc1" style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC1 · Fault detection</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">IQ waveforms → autoencoder → anomaly score</div>
</div>
<div data-id="uc2" style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC2 · Beam tuning</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">Quad settings → Cheetah → BO → proposal</div>
</div>
<div data-id="uc3" style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC3 · Surrogate</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">5 000 Cheetah calls → MLP → ~600× speedup</div>
</div>
</div>

::: notes
Road-map — present the full pipeline, then step through the highlighted sections before diving into each topic.
:::

---

## The ML pipeline — from data to inference {data-auto-animate=true}

<div data-id="pipe-row" style="display:flex; align-items:stretch; gap:0.22em; margin-top:6em; width:100%;">
<div data-id="p1" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.18); text-align:center; box-shadow:0 0 30px rgba(34,211,238,0.55);">
<div style="font-weight:700; font-size:0.76em; color:#22d3ee;">Raw Data</div>
<div style="font-size:0.62em; color:#94a3b8; margin-top:0.25em; line-height:1.35;">Waveforms · images<br>logs · set-points</div>
</div>
<div data-id="a1" style="align-self:center; color:#22d3ee; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p2" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.18); text-align:center; box-shadow:0 0 30px rgba(34,211,238,0.55);">
<div style="font-weight:700; font-size:0.76em; color:#22d3ee;">Pre-process</div>
<div style="font-size:0.62em; color:#94a3b8; margin-top:0.25em; line-height:1.35;">Clean · sync<br>normalise · resample</div>
</div>
<div data-id="a2" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em; opacity:0.35;">→</div>
<div data-id="p3" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.25); background:rgba(148,163,184,0.03); text-align:center; opacity:0.35;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Feature extract</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">FFT · Gaussian fit<br>statistics · domain</div>
</div>
<div data-id="a3" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em; opacity:0.35;">→</div>
<div data-id="p4" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.25); background:rgba(148,163,184,0.03); text-align:center; opacity:0.35;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Train &amp; validate</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">80/20 split<br>cross-val · AUC</div>
</div>
<div data-id="a4" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em; opacity:0.35;">→</div>
<div data-id="p5" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.25); background:rgba(148,163,184,0.03); text-align:center; opacity:0.35;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Deploy &amp; infer</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Sub-ms response<br>monitoring · retrain</div>
</div>
</div>

<div data-id="uc-row" style="display:flex; gap:0.35em; margin-top:2em; width:100%;">
<div data-id="uc1" style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05); opacity:0.12;">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC1 · Fault detection</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">IQ waveforms → autoencoder → anomaly score</div>
</div>
<div data-id="uc2" style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05); opacity:0.12;">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC2 · Beam tuning</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">Quad settings → Cheetah → BO → proposal</div>
</div>
<div data-id="uc3" style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05); opacity:0.12;">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC3 · Surrogate</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">5 000 Cheetah calls → MLP → ~600× speedup</div>
</div>
</div>

<div data-id="cap" style="text-align:center; margin-top:0.6em; font-size:0.82em; font-weight:600; color:#22d3ee;">
① Data quality &amp; preprocessing
</div>

::: notes
Data quality and preprocessing — cleaning, synchronising, and normalising raw accelerator signals. This is what the next slides cover.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:28%; margin-top:0.4em;">
<div data-id="p1" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Raw Data</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">Waveforms · images<br>logs · set-points</div>
</div>
<div data-id="a1" style="align-self:center; color:#64748b; font-size:0.85em; flex-shrink:0; padding:0 0.05em;">→</div>
<div data-id="p2" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Pre-process</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">Clean · sync<br>normalise · resample</div>
</div>
</div>

::: notes
Pause here — let the two boxes land. Next press reveals the full data quality content.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:28%; margin-top:0.4em;">
<div data-id="p1" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Raw Data</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">Waveforms · images<br>logs · set-points</div>
</div>
<div data-id="a1" style="align-self:center; color:#64748b; font-size:0.85em; flex-shrink:0; padding:0 0.05em;">→</div>
<div data-id="p2" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Pre-process</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">Clean · sync<br>normalise · resample</div>
</div>
</div>

::: {style="display:flex; gap:0.35em; margin:0.4em 0 0.35em 0;"}
::: {.highlight-box style="flex:1; padding:0.35em 0.55em; font-size:0.78em; margin:0;"}
**① Clean**
Remove corrupted pulses, stuck sensors, dropouts.
:::
::: {.fragment .highlight-box fragment-index=1 style="flex:1; padding:0.35em 0.55em; font-size:0.78em; margin:0;"}
**② Synchronise**
Align timestamps across RF, BPM, power supplies.
:::
::: {.fragment .highlight-box fragment-index=2 style="flex:1; padding:0.35em 0.55em; font-size:0.78em; margin:0;"}
**③ Normalise**
Scale to [−1, 1] — prevents one channel dominating.
:::
::: {.fragment .highlight-box fragment-index=3 style="flex:1; padding:0.35em 0.55em; font-size:0.78em; margin:0;"}
**④ Balance**
SRF faults < 0.1 % of pulses — naïve model learns to ignore them.
:::
:::

::: {style="position:relative;"}

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go
from plotly.subplots import make_subplots

np.random.seed(42)
t = np.linspace(0, 4*np.pi, 300)
base = np.sin(t)

# ① Clean — raw with noise + 2 outliers, de-spiked overlay
raw = base + 0.25 * np.random.randn(300)
raw[55] = 3.9
raw[195] = -3.6
cleaned = base + 0.08 * np.random.randn(300)

# ② Synchronise — two channels, one time-lagged
lag = 24
sigA = base
sigB = 0.85 * np.roll(base, lag)

# ③ Normalise — min-max scale cleaned signal to [−1, 1]
mn, mx = cleaned.min(), cleaned.max()
norm = 2.0 * (cleaned - mn) / (mx - mn) - 1.0

fig = make_subplots(
    rows=1, cols=4,
    subplot_titles=["① Clean", "② Synchronise", "③ Normalise", "④ Balance"],
    horizontal_spacing=0.035,
)

# Panel 1 — Clean
fig.add_trace(go.Scatter(x=t, y=raw, mode='lines',
    line=dict(color='#475569', width=1.0), showlegend=False), row=1, col=1)
fig.add_trace(go.Scatter(x=t, y=cleaned, mode='lines',
    line=dict(color='#22d3ee', width=1.4), showlegend=False), row=1, col=1)
fig.add_trace(go.Scatter(x=[t[55], t[195]], y=[raw[55], raw[195]], mode='markers',
    marker=dict(color='#f87171', size=11, symbol='x',
                line=dict(width=2.2, color='#f87171')), showlegend=False), row=1, col=1)

# Panel 2 — Synchronise
fig.add_trace(go.Scatter(x=t, y=sigA, mode='lines',
    line=dict(color='#60a5fa', width=1.3), showlegend=False), row=1, col=2)
fig.add_trace(go.Scatter(x=t, y=sigB, mode='lines',
    line=dict(color='#a78bfa', width=1.3, dash='dot'), showlegend=False), row=1, col=2)

# Panel 3 — Normalise
fig.add_trace(go.Scatter(x=t, y=norm, mode='lines',
    line=dict(color='#22d3ee', width=1.4), showlegend=False), row=1, col=3)
fig.add_hline(y=1,  line_dash="dot", line_color="#475569", opacity=0.7, row=1, col=3)
fig.add_hline(y=-1, line_dash="dot", line_color="#475569", opacity=0.7, row=1, col=3)

# Panel 4 — Balance (class imbalance, log scale)
fig.add_trace(go.Bar(x=['normal', 'fault'], y=[99900, 100],
    marker_color=['#334155', '#f87171'],
    text=['99.9 %', '0.1 %'], textposition='outside',
    textfont=dict(color='#e2e8f0', size=11), showlegend=False), row=1, col=4)

fig.update_xaxes(showgrid=False, showticklabels=False, zeroline=False)
fig.update_yaxes(showgrid=False, showticklabels=False, zeroline=False)
fig.update_yaxes(range=[-1.3, 1.3], row=1, col=3)
fig.update_yaxes(type='log', range=[1.4, 5.9], row=1, col=4)

fig.update_layout(
    height=300, autosize=True,
    paper_bgcolor='#0d1b2a', plot_bgcolor='#111f30',
    font=dict(color='#e2e8f0', size=11),
    margin=dict(t=30, b=8, l=8, r=8),
)
fig
```

<div style="position:absolute; left:0; right:0; top:26px; bottom:14px; display:flex; gap:1.1%; pointer-events:none;">
<div class="fragment fade-out" data-fragment-index="1" style="flex:1; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.06); box-shadow:0 0 22px rgba(34,211,238,0.5);"></div>
<div class="fragment current-visible" data-fragment-index="1" style="flex:1; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.06); box-shadow:0 0 22px rgba(34,211,238,0.5);"></div>
<div class="fragment current-visible" data-fragment-index="2" style="flex:1; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.06); box-shadow:0 0 22px rgba(34,211,238,0.5);"></div>
<div class="fragment current-visible" data-fragment-index="3" style="flex:1; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.06); box-shadow:0 0 22px rgba(34,211,238,0.5);"></div>
</div>

:::

<div class="fragment slide-callout-overlay" data-fragment-index="4">
<div class="callout-box">
A model trained on bad data learns bad behaviour.<br>
<span style="font-weight:400; font-size:0.82em; color:#94a3b8;">No algorithm compensates for this.</span>
</div>
</div>

::: notes
Four preprocessing steps, ordered by how often they are skipped and later regretted. Each press lights up its panel in the strip below. Clean — remove corrupted pulses, stuck sensors, dropouts (panel 1: grey raw with two red outliers, cyan de-spiked result). Synchronise — align timestamps across RF, BPMs, and power supplies, genuinely hard across subsystems with different clocks (panel 2: two channels, one lagged). Normalise to [−1, 1] — so one high-amplitude channel does not dominate the loss (panel 3: scaled between the dotted bounds). Balance — SRF faults are under 0.1% of pulses, so a naïve model reaches 99.9% "accuracy" by always predicting normal and detecting nothing (panel 4: the log-scale class bar makes the imbalance visceral). Punchline on the overlay: a model trained on bad data learns bad behaviour, and no algorithm downstream compensates for that.
:::

---

## {data-auto-animate=true}

<div data-id="pipe-row" style="display:flex; align-items:stretch; gap:0.22em; margin-top:6em; width:100%;">
<div data-id="p1" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Raw Data</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Waveforms · images<br>logs · set-points</div>
</div>
<div data-id="a1" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p2" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Pre-process</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Clean · sync<br>normalise · resample</div>
</div>
<div data-id="a2" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p3" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.18); text-align:center; box-shadow:0 0 30px rgba(34,211,238,0.55);">
<div style="font-weight:700; font-size:0.76em; color:#22d3ee;">Feature extract</div>
<div style="font-size:0.62em; color:#94a3b8; margin-top:0.25em; line-height:1.35;">FFT · Gaussian fit<br>statistics · domain</div>
</div>
<div data-id="a3" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em; opacity:0.35;">→</div>
<div data-id="p4" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.25); background:rgba(148,163,184,0.03); text-align:center; opacity:0.35;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Train &amp; validate</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">80/20 split<br>cross-val · AUC</div>
</div>
<div data-id="a4" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em; opacity:0.35;">→</div>
<div data-id="p5" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.25); background:rgba(148,163,184,0.03); text-align:center; opacity:0.35;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Deploy &amp; infer</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Sub-ms response<br>monitoring · retrain</div>
</div>
</div>

<div style="display:flex; gap:0.35em; margin-top:2em; width:100%;">
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC1 · Fault detection</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">IQ waveforms → autoencoder → anomaly score</div>
</div>
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC2 · Beam tuning</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">Quad settings → Cheetah → BO → proposal</div>
</div>
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC3 · Surrogate</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">5 000 Cheetah calls → MLP → ~600× speedup</div>
</div>
</div>

<div style="text-align:center; margin-top:0.6em; font-size:0.82em; font-weight:600; color:#22d3ee;">
② Feature engineering
</div>

::: notes
You are here — feature extraction. p1 and p2 animate back from the top-left into the pipeline row.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:16%; margin-top:0.4em;">
<div data-id="p3" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Feature extract</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">FFT · Gaussian fit<br>statistics · domain</div>
</div>
</div>

::: notes
p3 shrinks and flies to top-left. Everything else disappears. Next press reveals feature engineering content.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:16%; margin-top:0.4em;">
<div data-id="p3" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Feature extract</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">FFT · Gaussian fit<br>statistics · domain</div>
</div>
</div>

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go
from plotly.subplots import make_subplots

np.random.seed(7)
n = 256
t = np.linspace(0, 1, n) * 1000  # ms

# Simulated SRF cavity IQ: flat-top pulse with ramps
A = np.zeros(n)
A[30:220] = 1.0
A[30:60]  = np.linspace(0, 1, 30)
A[195:220] = np.linspace(1, 0, 25)
A += 0.03 * np.random.randn(n)

phase_drift = np.zeros(n)
phase_drift[30:220] = np.cumsum(0.003 * np.random.randn(190))
I = A * np.cos(phase_drift)
Q = A * np.sin(phase_drift)

mean_amp   = float(np.mean(A[30:220]))
phase_std  = float(np.std(phase_drift[30:220]) * 180 / np.pi)
fill_frac  = float(np.sum(A > 0.5) / n)

fig = make_subplots(
    rows=1, cols=2,
    subplot_titles=[
        "Raw IQ waveform — 256 samples / pulse",
        "Extracted features — 3 scalars",
    ],
    column_widths=[0.62, 0.38],
)

fig.add_trace(go.Scatter(x=t, y=I, mode='lines',
    line=dict(color='#60a5fa', width=1.2), name='I'), row=1, col=1)
fig.add_trace(go.Scatter(x=t, y=Q, mode='lines',
    line=dict(color='#a78bfa', width=1.2), name='Q'), row=1, col=1)
fig.add_trace(go.Scatter(x=t, y=A, mode='lines',
    line=dict(color='#22d3ee', width=2.2), name='|A| amplitude'), row=1, col=1)

fig.add_trace(go.Bar(
    x=['Mean<br>amplitude', 'Phase drift<br>(deg)', 'Fill<br>fraction'],
    y=[mean_amp, phase_std, fill_frac],
    marker_color=['#22d3ee', '#f87171', '#fb923c'],
    text=[f'{mean_amp:.3f}', f'{phase_std:.2f}°', f'{fill_frac:.2f}'],
    textposition='outside',
    textfont=dict(color='#e2e8f0', size=13),
    showlegend=False,
), row=1, col=2)

fig.update_xaxes(showgrid=True, gridcolor='#1e3048', zeroline=False,
    title_text="Time (ms)", row=1, col=1)
fig.update_yaxes(showgrid=True, gridcolor='#1e3048', zeroline=False, row=1, col=1)
fig.update_xaxes(showgrid=False, row=1, col=2)
fig.update_yaxes(showgrid=True, gridcolor='#1e3048', zeroline=False,
    range=[0, max(mean_amp, phase_std, fill_frac)*1.4], row=1, col=2)

fig.update_layout(
    height=360, autosize=True,
    paper_bgcolor='#0d1b2a', plot_bgcolor='#111f30',
    font=dict(color='#e2e8f0', size=11),
    legend=dict(bgcolor='rgba(0,0,0,0)', font=dict(size=10), x=0.02, y=0.96),
    margin=dict(t=28, b=8, l=10, r=10),
)
fig
```

::: {style="font-size:0.68em; color:#22d3ee; font-weight:600; text-align:center; margin:0.15em 0 0 0;"}
this case: 256 IQ samples → 3 scalars · 99 % compression, fault information retained
:::

::: {style="font-size:0.58em; color:#94a3b8; margin-top:0.3em; line-height:1.55; text-align:center;"}
**The feature map depends on the data** — RF waveform → **FFT peaks · Q · phase drift** &nbsp;·&nbsp; beam image → **Gaussian-fit σ, centroid, image moments** &nbsp;·&nbsp; bunch → **6-D phase-space stats (emittance, Twiss α/β)** &nbsp;·&nbsp; BPM stream → **orbit harmonics**
:::

<div class="fragment slide-callout-overlay">
<div class="callout-box">
Good features = simpler models + better generalisation.<br>
<span style="font-weight:400; font-size:0.82em; color:#94a3b8;">Domain knowledge beats brute force.</span>
</div>
</div>

::: notes
The whole point of feature engineering in one figure: for *this* case, 256 raw IQ samples collapse to three physically meaningful scalars — mean amplitude, phase drift, fill fraction — with no loss of the information that distinguishes faults (99% compression). The strip beneath makes the general point: the right feature map depends on the data — FFT peaks/Q/phase for an RF waveform, Gaussian-fit σ and image moments for a screen image, 6-D phase-space statistics (emittance, Twiss) for a bunch, orbit harmonics for a BPM stream. Good features mean smaller models, less data, better generalisation: domain knowledge beats brute force. Then flag the trade-off that sets up UC1: this is the classical route, whereas the autoencoder deliberately skips hand-crafted features and learns its own representation from the raw waveform. Neither is universally right — it depends on how much you trust your physics versus your data.
:::

---

## {data-auto-animate=true}

<div data-id="pipe-row" style="display:flex; align-items:stretch; gap:0.22em; margin-top:6em; width:100%;">
<div data-id="p1" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Raw Data</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Waveforms · images<br>logs · set-points</div>
</div>
<div data-id="a1" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p2" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Pre-process</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Clean · sync<br>normalise · resample</div>
</div>
<div data-id="a2" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p3" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Feature extract</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">FFT · Gaussian fit<br>statistics · domain</div>
</div>
<div data-id="a3" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p4" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.18); text-align:center; box-shadow:0 0 30px rgba(34,211,238,0.55);">
<div style="font-weight:700; font-size:0.76em; color:#22d3ee;">Train &amp; validate</div>
<div style="font-size:0.62em; color:#94a3b8; margin-top:0.25em; line-height:1.35;">80/20 split<br>cross-val · AUC</div>
</div>
<div data-id="a4" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em; opacity:0.35;">→</div>
<div data-id="p5" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.25); background:rgba(148,163,184,0.03); text-align:center; opacity:0.35;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Deploy &amp; infer</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Sub-ms response<br>monitoring · retrain</div>
</div>
</div>

<div style="display:flex; gap:0.35em; margin-top:2em; width:100%;">
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC1 · Fault detection</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">IQ waveforms → autoencoder → anomaly score</div>
</div>
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC2 · Beam tuning</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">Quad settings → Cheetah → BO → proposal</div>
</div>
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC3 · Surrogate</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">5 000 Cheetah calls → MLP → ~600× speedup</div>
</div>
</div>

<div style="text-align:center; margin-top:0.6em; font-size:0.82em; font-weight:600; color:#22d3ee;">
③ Train &amp; validate
</div>

::: notes
Third stage — train and validate. p4 lights up in the pipeline; the next press shrinks it into the top-left corner before the detail slide.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:16%; margin-top:0.4em;">
<div data-id="p4" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Train &amp; validate</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">80/20 split<br>cross-val · AUC</div>
</div>
</div>

::: notes
p4 flies to the top-left corner and everything else clears. Next press brings in the train-and-validate detail.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:16%; margin-top:0.4em;">
<div data-id="p4" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Train &amp; validate</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">80/20 split<br>cross-val · AUC</div>
</div>
</div>

::: {style="text-align:center; font-size:0.72em; color:#cbd5e1; margin:0.05em 0 0.45em 0;"}
**The task decides what "validate" means — and which metric.**
:::

<div style="display:flex; gap:0.4em; margin:0.2em 0 0.4em 0; align-items:stretch;">
<div class="highlight-box" style="flex:1; padding:0.4em 0.6em; font-size:0.66em; margin:0;">
<strong>Anomaly detection</strong> · UC1 <span style="color:#94a3b8;">unsupervised autoencoder</span>
<div style="border-top:1px solid rgba(148,163,184,0.25); margin:0.3em 0;"></div>
Validate on held-out <strong>normal + unseen fault types</strong>
<div style="margin-top:0.25em; color:#34d399;">✓ ROC-AUC · PR-AUC · F1</div>
<div style="color:#f87171;">✗ accuracy — 99.9 % normal hides everything</div>
</div>
<div class="fragment highlight-box" data-fragment-index="1" style="flex:1; padding:0.4em 0.6em; font-size:0.66em; margin:0;">
<strong>Surrogate regression</strong> · UC3 <span style="color:#94a3b8;">supervised MLP</span>
<div style="border-top:1px solid rgba(148,163,184,0.25); margin:0.3em 0;"></div>
Validate on inputs <strong>outside the training points</strong>
<div style="margin-top:0.25em; color:#34d399;">✓ R² · RMSE · residual / σ</div>
<div style="color:#f87171;">✗ extrapolation past the training envelope</div>
</div>
<div class="fragment highlight-box" data-fragment-index="2" style="flex:1; padding:0.4em 0.6em; font-size:0.66em; margin:0;">
<strong>Optimisation</strong> · UC2 <span style="color:#94a3b8;">Bayesian optimisation</span>
<div style="border-top:1px solid rgba(148,163,184,0.25); margin:0.3em 0;"></div>
No train/test split — judge <strong>best-so-far vs budget, across seeds</strong>
<div style="margin-top:0.25em; color:#34d399;">✓ regret · convergence · reproducibility</div>
<div style="color:#f87171;">✗ trusting one lucky run</div>
</div>
</div>

<div class="fragment slide-callout-overlay" data-fragment-index="3">
<div class="callout-box">
Same word, three protocols.<br>
<span style="font-weight:400; font-size:0.82em; color:#94a3b8;">Anomaly & surrogate split the data; optimisation measures regret over a budget — there is no test set.</span>
</div>
</div>

::: notes
Before any loss curve, fix the most common confusion: "validate" is not one thing. Three task types, three protocols. Anomaly detection (UC1) is unsupervised — you validate the autoencoder on held-out normal data and, critically, on fault types it never saw in training; the metrics are ROC-AUC, precision-recall, F1, never raw accuracy, which is meaningless at 99.9% normal. Surrogate regression (UC3) is supervised — you hold out input points and check R², RMSE, and residuals against the model's own uncertainty; the real failure mode is extrapolation outside the training envelope. Optimisation (UC2) is different in kind — there is no test set at all; you judge a Bayesian-optimisation run by its regret, its convergence against the evaluation budget, and whether it reproduces across random seeds, not by any single lucky run. The takeaway box: same word, three protocols. The next slide drills into the split-and-overfitting story, which applies to the first two.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:16%; margin-top:0.4em;">
<div data-id="p4" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Train &amp; validate</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">80/20 split<br>cross-val · AUC</div>
</div>
</div>

::: {style="text-align:center; font-size:0.7em; color:#cbd5e1; margin:0.05em 0 0.4em 0;"}
**Train / validate / test — and the overfitting trap** <span style="color:#64748b; font-size:0.85em;">(supervised tasks)</span>
:::

<div style="display:flex; width:100%; height:2.3em; border-radius:7px; overflow:hidden; font-size:0.6em; font-weight:700; color:#0d1b2a; margin-bottom:0.15em;">
<div style="flex:70; background:#60a5fa; display:flex; align-items:center; justify-content:center;">Train · 70 %</div>
<div style="flex:15; background:#22d3ee; display:flex; align-items:center; justify-content:center;">Val · 15 %</div>
<div style="flex:15; background:#fbbf24; display:flex; align-items:center; justify-content:center;">Test · 15 %</div>
</div>
<div style="display:flex; width:100%; font-size:0.5em; color:#94a3b8; text-align:center; margin-bottom:0.4em;">
<div style="flex:70;">fit the weights</div>
<div style="flex:15;">tune &amp; early-stop</div>
<div style="flex:15;">touch <em>once</em> — unbiased estimate</div>
</div>

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go

np.random.seed(42)
epochs = np.arange(1, 81)

# Same decay so validation stays ABOVE train at every epoch (never dips below).
decay = 0.72 * np.exp(-epochs / 12)

# Train loss keeps falling — the model fits the TRAINING set ever better
train_loss = decay + 0.020 + 0.005 * np.random.randn(80)

# Validation loss is U-shaped — falls with train, bottoms out, then RISES (overfitting)
val_smooth = decay + 0.050 + 0.0026 * np.clip(epochs - 16, 0, None)
val_loss   = val_smooth + 0.006 * np.random.randn(80)

best     = int(np.argmin(val_smooth)) + 1     # early stop = validation minimum
val_best  = float(val_smooth[best - 1])
test_loss = val_best + 0.013                   # TEST set — one honest evaluation at the best model

fig = go.Figure()
# overfitting region (after the validation minimum)
fig.add_vrect(x0=best, x1=80, fillcolor='rgba(248,113,113,0.06)', line_width=0,
    annotation_text='overfitting', annotation_position='top right',
    annotation_font=dict(color='#f87171', size=10))
fig.add_trace(go.Scatter(x=epochs, y=train_loss, name='Train (70 %)',
    line=dict(color='#60a5fa', width=1.8)))
fig.add_trace(go.Scatter(x=epochs, y=val_loss, name='Validation (15 %)',
    line=dict(color='#22d3ee', width=1.8)))
# best model / early stop at the validation minimum
fig.add_vline(x=best, line_dash='dot', line_color='#fbbf24', opacity=0.85)
fig.add_annotation(x=best, y=0.02, text='best model<br>(early stop)', showarrow=False,
    font=dict(color='#fbbf24', size=9), xanchor='center', yanchor='bottom')
# test set — a single point, evaluated once at the chosen model
fig.add_trace(go.Scatter(x=[best], y=[test_loss], mode='markers', name='Test (15 %) — once',
    marker=dict(color='#fbbf24', size=13, symbol='star',
                line=dict(color='#0d1b2a', width=1.2))))

fig.update_layout(
    height=270, autosize=True,
    paper_bgcolor='#0d1b2a', plot_bgcolor='#111f30',
    font=dict(color='#e2e8f0', size=11),
    legend=dict(bgcolor='rgba(0,0,0,0)', font=dict(size=10), x=0.60, y=0.97),
    xaxis=dict(title='Epoch', showgrid=True, gridcolor='#1e3048', zeroline=False),
    yaxis=dict(title='MSE loss', showgrid=True, gridcolor='#1e3048', zeroline=False),
    margin=dict(t=10, b=42, l=58, r=15),
)
fig
```

<div class="fragment slide-callout-overlay">
<div class="concept-callout-box">
<div class="concept-callout-label">⚡&nbsp; Definition — Epoch</div>
<div>
One <strong>epoch</strong> = one complete forward + backward pass over every training sample.<br>
<span style="font-size:0.88em; color:#94a3b8;">
Example: 10 000 pulses, batch 32 → 313 weight updates per epoch.<br>
Training halts when validation loss stops improving — the dashed line marks that minimum (early stopping).
</span>
</div>
</div>
</div>

<div class="fragment slide-callout-overlay">
<div class="callout-box">
Validate on data the model has never seen — and on faults it has never seen.<br>
<span style="font-weight:400; font-size:0.82em; color:#94a3b8;">Temporal splits matter more than random splits in accelerator data — and stratify to preserve the &lt; 0.1 % fault rate.</span>
</div>
</div>

::: notes
The three-way split, in order of who touches it: train fits the weights (blue, keeps falling); validation tunes hyper-parameters and triggers early stopping (cyan, U-shaped); test is the amber star — touched exactly once, at the chosen model, for an unbiased estimate (every extra peek leaks it into your choices). The loss curve is the overfitting story: train loss keeps falling while validation loss bottoms out and then turns back up — that widening gap is the model memorising noise, and early stopping freezes it at the validation minimum, where we read the test point. Define "epoch" from the concept box if the audience is mixed. Two accelerator-specific cautions on the last overlay: prefer temporal splits over random ones — random splits leak information between time-correlated neighbouring pulses and flatter the model — and stratify so the sub-0.1% fault rate survives in every split. With only hundreds of labelled faults, 5-fold cross-validation on the training portion is the practical way to get a stable estimate.
:::

---

## {data-auto-animate=true}

<div data-id="pipe-row" style="display:flex; align-items:stretch; gap:0.22em; margin-top:6em; width:100%;">
<div data-id="p1" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Raw Data</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Waveforms · images<br>logs · set-points</div>
</div>
<div data-id="a1" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p2" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Pre-process</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">Clean · sync<br>normalise · resample</div>
</div>
<div data-id="a2" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p3" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Feature extract</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">FFT · Gaussian fit<br>statistics · domain</div>
</div>
<div data-id="a3" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p4" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:1.5px solid rgba(148,163,184,0.35); background:rgba(148,163,184,0.04); text-align:center;">
<div style="font-weight:700; font-size:0.76em; color:#94a3b8;">Train &amp; validate</div>
<div style="font-size:0.62em; color:#475569; margin-top:0.25em; line-height:1.35;">80/20 split<br>cross-val · AUC</div>
</div>
<div data-id="a4" style="align-self:center; color:#64748b; font-size:1.15em; flex-shrink:0; padding:0 0.1em;">→</div>
<div data-id="p5" style="flex:1; padding:0.6em 0.3em; border-radius:8px; border:2px solid #22d3ee; background:rgba(34,211,238,0.18); text-align:center; box-shadow:0 0 30px rgba(34,211,238,0.55);">
<div style="font-weight:700; font-size:0.76em; color:#22d3ee;">Deploy &amp; infer</div>
<div style="font-size:0.62em; color:#94a3b8; margin-top:0.25em; line-height:1.35;">Sub-ms response<br>monitoring · retrain</div>
</div>
</div>

<div style="display:flex; gap:0.35em; margin-top:2em; width:100%;">
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC1 · Fault detection</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">IQ waveforms → autoencoder → anomaly score</div>
</div>
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC2 · Beam tuning</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">Quad settings → Cheetah → BO → proposal</div>
</div>
<div style="flex:1; padding:0.35em 0.5em; border-radius:6px; border:1px solid rgba(180,160,140,0.38); background:rgba(180,160,140,0.05);">
<div style="font-size:0.65em; font-weight:600; color:#b0a090;">UC3 · Surrogate</div>
<div style="font-size:0.56em; color:#475569; margin-top:0.1em;">5 000 Cheetah calls → MLP → ~600× speedup</div>
</div>
</div>

<div style="text-align:center; margin-top:0.6em; font-size:0.82em; font-weight:600; color:#22d3ee;">
④ Deploy &amp; infer
</div>

::: notes
Fourth and final stage — deploy and infer. p5 lights up; the next press shrinks it before the detail slide.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:16%; margin-top:0.4em;">
<div data-id="p5" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Deploy &amp; infer</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">Sub-ms response<br>monitoring · retrain</div>
</div>
</div>

::: notes
p5 flies to the corner; next press brings in the deployment detail.
:::

---

## {data-auto-animate=true}

<div style="display:flex; gap:0.25em; width:16%; margin-top:0.4em;">
<div data-id="p5" style="flex:1; padding:0.3em 0.2em; border-radius:6px; border:1px solid rgba(148,163,184,0.45); background:rgba(148,163,184,0.06); text-align:center;">
<div style="font-weight:700; font-size:0.58em; color:#94a3b8;">Deploy &amp; infer</div>
<div style="font-size:0.46em; color:#475569; margin-top:0.2em; line-height:1.3;">Sub-ms response<br>monitoring · retrain</div>
</div>
</div>

::: {style="display:flex; gap:0.3em; margin:0.4em 0 0.35em 0;"}
::: {.highlight-box style="flex:1; padding:0.3em 0.5em; font-size:0.68em; margin:0;"}
**① Latency budget**
Set by the *control loop*, not the model: fast feedback may need sub-ms, daily tuning tolerates seconds.
:::
::: {.fragment .highlight-box style="flex:1; padding:0.3em 0.5em; font-size:0.68em; margin:0;"}
**② Export &amp; serve**
Deterministic runtime (e.g. ONNX, TorchScript) embeddable in EPICS / TANGO / DOOCS — no Python in the loop.
:::
::: {.fragment .highlight-box style="flex:1; padding:0.3em 0.5em; font-size:0.68em; margin:0;"}
**③ Compress to hardware**
Pruning + quantisation (FP32→INT8) cut latency — at some accuracy cost; trade-off depends on CPU / GPU / FPGA.
:::
::: {.fragment .highlight-box style="flex:1; padding:0.3em 0.5em; font-size:0.68em; margin:0;"}
**④ Monitor &amp; retrain**
Track output rate and input distributions; retrain when drift crosses a threshold.
:::
:::

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

models    = ['Threshold<br>rule', 'Autoencoder<br>(ONNX)', 'GP<br>Surrogate', 'Transformer<br>(full)']
latencies = [0.01, 0.18, 2.4, 14.0]
colors    = ['#94a3b8', '#22d3ee', '#60a5fa', '#a78bfa']

fig = go.Figure()
fig.add_trace(go.Bar(
    x=models, y=latencies,
    marker_color=colors,
    text=[f'{v} ms' for v in latencies],
    textposition='outside',
    textfont=dict(color='#e2e8f0', size=12),
))
fig.add_hline(y=10, line_dash='dot', line_color='#fbbf24', opacity=0.8,
    annotation_text='  example control-loop deadline',
    annotation_font=dict(color='#fbbf24', size=10),
    annotation_position='top left')

fig.update_layout(
    height=290, autosize=True,
    paper_bgcolor='#0d1b2a', plot_bgcolor='#111f30',
    font=dict(color='#e2e8f0', size=11),
    xaxis=dict(showgrid=False, zeroline=False),
    yaxis=dict(title='Inference latency (ms)', type='log',
               showgrid=True, gridcolor='#1e3048', zeroline=False),
    margin=dict(t=10, b=10, l=70, r=15),
    showlegend=False,
)
fig
```

<div class="fragment slide-callout-overlay">
<div class="callout-box">
A model in a notebook is not a deployed model.<br>
<span style="font-weight:400; font-size:0.82em; color:#94a3b8;">Latency, reliability and drift monitoring are engineering problems.</span>
</div>
</div>

::: notes
This is where most accelerator-ML projects actually fail. Four constraints, none about the model's accuracy. Latency budget — set by the *control loop*, not the model: a fast feedback loop may demand sub-millisecond inference, while daily tuning tolerates seconds, so match the model to the loop's deadline (don't quote a universal number). Export and serve — convert to a deterministic runtime; ONNX and TorchScript are two examples, embeddable in EPICS / TANGO / DOOCS with no Python interpreter in the loop. Compress to the hardware — pruning and quantisation (e.g. FP32→INT8) cut latency and memory at some accuracy cost, and the right trade-off depends on whether you deploy on CPU, GPU, or FPGA. Monitor and retrain — track output rate and input distributions, retrain when drift crosses a threshold. The log-scale chart is illustrative (the dashed line is an *example* deadline). Closing line: a model in a notebook is not a deployed model — latency, precision, reliability, and drift are engineering problems, not modelling ones.
:::

# Practical Applications {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
Three use cases · Three live notebooks
:::

::: {style="font-size: 0.95em; color: #334155; margin-top: 0.5em; line-height: 1.9;"}
Use case 1 — RF fault detection &nbsp;·&nbsp; Use case 2 — Beam tuning &nbsp;·&nbsp; Use case 3 — Surrogate models
:::

::: notes
Three back-to-back use cases, each self-contained. Same structure: problem → method → result → notebook. Companion notebooks are open on Colab.
:::

# Use Case 1 {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
RF Fault Detection
:::

::: {style="font-size: 1em; color: #334155;"}
Autoencoders on cavity IQ waveforms · Notebook 01
:::

::: notes
~11 minutes for UC1. The problem: rare RF faults with no labels. The method: an autoencoder trained on normal pulses only, with the anomaly score being the reconstruction error. Notebook 01 is live on Colab. Set expectations honestly here — be explicit later that this autoencoder is our own illustration on synthetic data, not the CEBAF production system, which is a supervised classifier (Tennant 2020).
:::

---

## The problem: rare faults, no labels

<div class="stepper"><span class="on">① Problem</span> · <span>② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

:::: {.columns style="margin-top:0.3em; gap:1.5em;"}
::: {.column width="54%"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="1"}
Every RF pulse → one **IQ waveform** per cavity at **~1 kHz**.
Clinical machines are **safe by design** — faults are rare.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="2"}
Supervised classifiers need thousands of labelled fault examples per class — **unavailable** in clinical operation.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em;" fragment-index="3"}
**Solution:** train on normal pulses only.
Anomaly score = reconstruction error.
:::

:::
::: {.column width="44%"}

<div style="display:grid;">

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.8em 0;" fragment-index="1"}
<span style="font-size:2.2em; font-weight:800; color:#22d3ee; line-height:1.0;">~1 kHz</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;">IQ samples per cavity per second<br>1M+ pulses/day in clinical operation</span>
:::

::: {.fragment .current-visible style="grid-area:1/1; padding:0.8em 0;" fragment-index="2"}
<div style="font-size:0.68em; font-weight:600; color:#22d3ee; margin-bottom:0.3em;">Four fault classes — superconducting cavity</div>

| Class | Signature |
|---|---|
| **Normal** | Clean trapezoid · Q ≈ 0 |
| **Quench** | Sudden drop mid flat-top |
| **Detuning** | Off-resonance · large Q |
| **Multipacting** | Oscillations on flat-top |

:::

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.8em 0;" fragment-index="3"}
<span style="font-size:1.8em; font-weight:800; color:#22d3ee; line-height:1.2;">No fault labels<br>needed</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;">Anomaly detection trains on normal only</span>
:::

</div>

:::
::::

::: notes
Frame the constraint honestly. Each cavity produces roughly 1 kHz of IQ waveforms — over a million pulses a day — but a clinical machine is safe by design, so genuine faults are vanishingly rare. That kills supervised classification: you cannot collect thousands of labelled examples per fault class in clinical operation. The reframe is the key insight: do not try to classify faults, instead model what "normal" looks like and flag anything that deviates. The four fault classes in the table — quench, detuning, multipacting, plus normal — are what we synthesise in the notebook to test the detector. The detector itself never sees them during training; it only ever learns normal.
:::

---

## What the waveforms look like

<div class="stepper"><span class="on">① Problem</span> · <span>② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go
from plotly.subplots import make_subplots

def _env(t, rise=0.10, fall=0.85):
    return np.piecewise(t.astype(float),
        [t < rise, (t >= rise) & (t <= fall), t > fall],
        [lambda t: t/rise, 1.0, lambda t: (1-t)/(1-fall)])

t = np.linspace(0, 1, 256)
noise = 0.025

def normal(seed=0):
    rng = np.random.default_rng(seed)
    env = _env(t)
    return env + rng.normal(0, noise, 256), rng.normal(0, noise, 256)

def quench(seed=1):
    rng = np.random.default_rng(seed)
    I, Q = normal(seed); qp = rng.integers(80, 160)
    I[qp:] = rng.normal(0, noise/2, 256-qp)
    Q[qp:] = rng.normal(0, noise/2, 256-qp)
    return I, Q

def detuning(seed=2):
    rng = np.random.default_rng(seed)
    env = _env(t); freq = rng.uniform(3, 7)
    return (env*np.cos(2*np.pi*freq*t)+rng.normal(0,noise,256),
            env*np.sin(2*np.pi*freq*t)+rng.normal(0,noise,256))

def multipacting(seed=3):
    rng = np.random.default_rng(seed)
    I, Q = normal(seed); s, e = 40, 210
    freq = rng.uniform(6, 12); amp = rng.uniform(0.12, 0.22)
    tt = np.linspace(0, 1, e-s)
    I[s:e] += amp*np.sin(2*np.pi*freq*tt)
    Q[s:e] += amp*np.cos(2*np.pi*freq*tt)
    return I, Q

cases = [("Normal", normal, "#22d3ee"), ("Quench", quench, "#f87171"),
         ("Detuning", detuning, "#fb923c"), ("Multipacting", multipacting, "#a78bfa")]

fig = make_subplots(rows=2, cols=4,
    subplot_titles=[f"{n} — I" for n,_,_ in cases]+[f"{n} — Q" for n,_,_ in cases],
    vertical_spacing=0.12, horizontal_spacing=0.06)

for col, (name, fn, color) in enumerate(cases, 1):
    I, Q = fn()
    fig.add_trace(go.Scatter(x=t, y=I, line=dict(color=color, width=1.5),
        name=name, showlegend=(col==1)), row=1, col=col)
    fig.add_trace(go.Scatter(x=t, y=Q, line=dict(color=color, width=1.5),
        showlegend=False), row=2, col=col)

fig.update_layout(height=510, autosize=True, paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=11), margin=dict(l=30, r=10, t=50, b=10),
    legend=dict(orientation="h", y=1.08, x=0.5, xanchor="center"))
fig.update_xaxes(showgrid=False, zeroline=False, color="#475569")
fig.update_yaxes(showgrid=True, gridcolor="#1e3048", zeroline=False, color="#475569")
fig
```

::: notes
Row 1 = I (in-phase), Row 2 = Q (quadrature). Normal: clean trapezoid, Q near zero. Detuning: strong Q. Quench: sharp drop. Multipacting: oscillations.
:::

---

## Anomaly detection — method landscape

<div class="stepper"><span>① Problem</span> · <span class="on">② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

# x: training data volume needed (0=minimal → 2=large dataset)
# y: suitability for rare-event / SRF fault detection (1=poor → 5=excellent)
methods = [
    ("Threshold rules",  0.00, 1.1, 20, "#475569", "bottom right"),
    ("PCA anomaly",      0.30, 2.0, 22, "#64748b", "top right"),
    ("KDE / GMM",        0.55, 2.3, 20, "#64748b", "bottom right"),
    ("Isolation Forest", 0.70, 2.9, 26, "#94a3b8", "top left"),
    ("One-class SVM",    0.90, 2.5, 24, "#94a3b8", "bottom right"),
    ("LSTM-AE",          1.60, 3.9, 26, "#a78bfa", "bottom right"),
    ("VAE",              1.80, 4.6, 30, "#a78bfa", "top right"),
    ("Autoencoder ★",   1.20, 4.3, 42, "#22d3ee", "top left"),
]

fig = go.Figure()
for name, x, y, size, color, pos in methods:
    is_pick = "★" in name
    fig.add_trace(go.Scatter(
        x=[x], y=[y], mode="markers+text",
        marker=dict(size=size, color=color,
            line=dict(color="#e2e8f0" if is_pick else "#0d1b2a",
                      width=2.5 if is_pick else 1.5)),
        text=[f"<b>{name}</b>" if is_pick else name],
        textposition=pos,
        textfont=dict(size=12 if is_pick else 10, color=color),
        showlegend=False,
    ))

fig.update_layout(
    height=530, autosize=True, paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=11),
    xaxis=dict(title="Training data volume required",
               range=[-0.3, 2.5],
               tickvals=[0, 1, 2],
               ticktext=["Minimal<br>(unsupervised)", "Moderate<br>(normal data only)", "Large<br>(fault labels)"],
               showgrid=True, gridcolor="#1e3048", zeroline=False),
    yaxis=dict(title="Suitability for rare-event detection",
               range=[0.6, 5.4],
               tickvals=[1, 2, 3, 4, 5],
               ticktext=["Poor", "Fair", "Moderate", "Good", "Excellent"],
               showgrid=True, gridcolor="#1e3048", zeroline=False),
    margin=dict(l=90, r=30, t=20, b=70),
)
fig
```

::: {.key-result style="margin-top: 0.3em; font-size: 0.78em;"}
**This lecture:** Autoencoder — unsupervised, normal-data only, scales to 1 kHz waveform streams.
:::

::: notes
The same decision map as before, zoomed into anomaly detection. x is the training data required, y is suitability for rare-event detection. The autoencoder (cyan star) sits in the sweet spot: it trains on normal data only, needs no fault labels, and scales to kHz waveform streams. The classical methods on the left — threshold rules, PCA, isolation forest — are cheaper but weaker on high-dimensional waveforms. The VAE and LSTM-autoencoder top-right are more powerful but more data-hungry and harder to deploy. We pick the autoencoder as the best balance for a clinical machine.
:::

---

## The autoencoder — architecture

<div class="stepper"><span>① Problem</span> · <span class="on">② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

:::::: {.columns}
::::: {.column width="56%"}

:::: {style="display:grid;"}

::: {style="grid-area:1/1;"}
```{.python}
class RFAutoencoder(nn.Module):
    def __init__(self, latent_dim=16):
        super().__init__()
        self.encoder = nn.Sequential(
            nn.Conv1d(2, 32, kernel_size=8, stride=4),
            nn.GELU(),
            nn.Conv1d(32, 64, kernel_size=4, stride=2),
            nn.GELU(),
            nn.Flatten(),
            nn.Linear(64 * 32, latent_dim),  # bottleneck
        )
        self.decoder = nn.Sequential(...)    # mirror
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="1"}
```{.python code-line-numbers="4-11"}
class RFAutoencoder(nn.Module):
    def __init__(self, latent_dim=16):
        super().__init__()
        self.encoder = nn.Sequential(
            nn.Conv1d(2, 32, kernel_size=8, stride=4),
            nn.GELU(),
            nn.Conv1d(32, 64, kernel_size=4, stride=2),
            nn.GELU(),
            nn.Flatten(),
            nn.Linear(64 * 32, latent_dim),  # bottleneck
        )
        self.decoder = nn.Sequential(...)    # mirror
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="2"}
```{.python code-line-numbers="5,7"}
class RFAutoencoder(nn.Module):
    def __init__(self, latent_dim=16):
        super().__init__()
        self.encoder = nn.Sequential(
            nn.Conv1d(2, 32, kernel_size=8, stride=4),
            nn.GELU(),
            nn.Conv1d(32, 64, kernel_size=4, stride=2),
            nn.GELU(),
            nn.Flatten(),
            nn.Linear(64 * 32, latent_dim),  # bottleneck
        )
        self.decoder = nn.Sequential(...)    # mirror
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="3"}
```{.python code-line-numbers="10"}
class RFAutoencoder(nn.Module):
    def __init__(self, latent_dim=16):
        super().__init__()
        self.encoder = nn.Sequential(
            nn.Conv1d(2, 32, kernel_size=8, stride=4),
            nn.GELU(),
            nn.Conv1d(32, 64, kernel_size=4, stride=2),
            nn.GELU(),
            nn.Flatten(),
            nn.Linear(64 * 32, latent_dim),  # bottleneck
        )
        self.decoder = nn.Sequential(...)    # mirror
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="4"}
```{.python code-line-numbers="12"}
class RFAutoencoder(nn.Module):
    def __init__(self, latent_dim=16):
        super().__init__()
        self.encoder = nn.Sequential(
            nn.Conv1d(2, 32, kernel_size=8, stride=4),
            nn.GELU(),
            nn.Conv1d(32, 64, kernel_size=4, stride=2),
            nn.GELU(),
            nn.Flatten(),
            nn.Linear(64 * 32, latent_dim),  # bottleneck
        )
        self.decoder = nn.Sequential(...)    # mirror
```
:::

::::

::: {style="font-size:0.62em; color:#94a3b8; margin-top:0.3em;"}
Input `(batch, 2, 256)` — I and Q as two channels
:::

:::::
::::: {.column width="44%"}

:::: {style="font-size:0.62em; padding-top:0.3em;"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="1"}
**① Encoder · lines 4–11** — two `Conv1d` stages compress the `(2, 256)` waveform.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="2"}
**② 1D convolutions · lines 5, 7** — capture local features (rise, drop, oscillations) with few parameters.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="3"}
**③ Bottleneck · line 10** — the pulse is squeezed through 16 numbers; the net must learn what *normal* looks like.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="4"}
**④ Decoder · line 12** — mirrors the encoder, rebuilding `(2, 256)` from the 16-D code.
:::

::::

:::::
::::::

:::: {.fragment style="text-align:center; margin-top:0.3em;" fragment-index="5"}
::: {.key-result style="display:inline-block; font-size:0.86em;"}
**Anomaly score = reconstruction error** · Normal → low · Faults → high
:::
::::

<div style="display:flex; align-items:center; justify-content:center; gap:0.3em; margin-top:0.3em; font-family:'IBM Plex Mono',monospace;">
<div style="width:54px; height:52px; background:rgba(34,211,238,0.18); border:1px solid #22d3ee; border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:0.42em; color:#e2e8f0;">2×256</div>
<span style="color:#64748b;">→</span>
<div style="width:46px; height:40px; background:rgba(34,211,238,0.15); border:1px solid rgba(34,211,238,0.6); border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:0.42em; color:#cbd5e1;">32 ch</div>
<span style="color:#64748b;">→</span>
<div style="width:46px; height:30px; background:rgba(34,211,238,0.12); border:1px solid rgba(34,211,238,0.45); border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:0.42em; color:#cbd5e1;">64 ch</div>
<span style="color:#64748b;">→</span>
<div style="width:46px; height:22px; background:rgba(251,191,36,0.22); border:2px solid #fbbf24; border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:0.44em; color:#fbbf24; font-weight:700;">16</div>
<span style="color:#64748b;">→</span>
<div style="width:46px; height:30px; background:rgba(96,165,250,0.12); border:1px solid rgba(96,165,250,0.45); border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:0.42em; color:#cbd5e1;">64 ch</div>
<span style="color:#64748b;">→</span>
<div style="width:46px; height:40px; background:rgba(96,165,250,0.15); border:1px solid rgba(96,165,250,0.6); border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:0.42em; color:#cbd5e1;">32 ch</div>
<span style="color:#64748b;">→</span>
<div style="width:54px; height:52px; background:rgba(96,165,250,0.18); border:1px solid #60a5fa; border-radius:4px; display:flex; align-items:center; justify-content:center; font-size:0.42em; color:#e2e8f0;">2×256</div>
</div>
<div style="display:flex; justify-content:center; gap:4.5em; font-size:0.5em; margin-top:0.2em;">
<span style="color:#22d3ee;">◀ encoder · compress</span>
<span style="color:#fbbf24;">bottleneck</span>
<span style="color:#60a5fa;">decoder · reconstruct ▶</span>
</div>

::: notes
**Arrow-driven walk-through.** Each right-arrow press highlights the matching code lines on the left and lights up the corresponding point on the right; earlier points stay visible but dimmed (half-faded), so the list accumulates. The funnel below stays up throughout as the anchor. ① Encoder — two Conv1d stages compress the 2×256 input. ② 1D convolutions slide along the time axis, capturing local structure (rise, drop, oscillations) with far fewer parameters than dense layers. ③ Bottleneck — the whole pulse is squeezed through 16 numbers, so the network can only store a compact model of *normal*; this dimension is the critical hyperparameter (too large and it reconstructs faults too, killing the signal). ④ Decoder mirrors the encoder. Final press: the punchline — anomaly score = reconstruction error, low for normal, high for faults.
:::

---

## Training and result

<div class="stepper"><span>① Problem</span> · <span>② Method</span> · <span class="on">③ Results</span> · <span>④ Notebook</span></div>

:::::: {.columns}
::::: {.column width="50%"}

:::: {style="display:grid;"}

::: {style="grid-area:1/1;"}
```{.python}
# Train on NORMAL pulses only
X_normal = X_train[y_train == 0]
for epoch in range(50):
    for (batch,) in loader:
        loss = ((batch - model(batch))**2).mean()
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

# Score every pulse at inference
with torch.no_grad():
    scores = ((X - model(X))**2).mean(dim=(1,2))
threshold = scores[normal_mask].quantile(0.95)
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="1"}
```{.python code-line-numbers="1-2"}
# Train on NORMAL pulses only
X_normal = X_train[y_train == 0]
for epoch in range(50):
    for (batch,) in loader:
        loss = ((batch - model(batch))**2).mean()
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

# Score every pulse at inference
with torch.no_grad():
    scores = ((X - model(X))**2).mean(dim=(1,2))
threshold = scores[normal_mask].quantile(0.95)
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="2"}
```{.python code-line-numbers="3-8"}
# Train on NORMAL pulses only
X_normal = X_train[y_train == 0]
for epoch in range(50):
    for (batch,) in loader:
        loss = ((batch - model(batch))**2).mean()
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

# Score every pulse at inference
with torch.no_grad():
    scores = ((X - model(X))**2).mean(dim=(1,2))
threshold = scores[normal_mask].quantile(0.95)
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="3"}
```{.python code-line-numbers="10-13"}
# Train on NORMAL pulses only
X_normal = X_train[y_train == 0]
for epoch in range(50):
    for (batch,) in loader:
        loss = ((batch - model(batch))**2).mean()
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

# Score every pulse at inference
with torch.no_grad():
    scores = ((X - model(X))**2).mean(dim=(1,2))
threshold = scores[normal_mask].quantile(0.95)
```
:::

::::

:::: {style="font-size:0.6em; margin-top:0.4em;"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="1"}
**① Train on *normal* only · lines 1–2** — the model never sees a fault.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="2"}
**② Reconstruction loss · lines 3–8** — minimise the MSE between each pulse and its rebuild.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em;" fragment-index="3"}
**③ Unsupervised threshold · lines 10–13** — flag pulses above the 95th percentile of *normal* errors. No fault labels used.
:::

::::

:::::
::::: {.column width="50%"}

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go

np.random.seed(42)
normal_s = np.random.lognormal(-2.6, 0.4, 200)
fault_s  = np.random.lognormal(-0.7, 0.6, 600)
thresh   = np.percentile(normal_s, 95)

fig = go.Figure()
fig.add_trace(go.Histogram(x=normal_s, xbins=dict(start=0, end=0.55, size=0.009),
    name="Normal", marker_color="#22d3ee", opacity=0.75))
fig.add_trace(go.Histogram(x=fault_s.clip(max=0.55), xbins=dict(start=0, end=0.55, size=0.009),
    name="Faults", marker_color="#f87171", opacity=0.75))
fig.add_vline(x=thresh, line_dash="dash", line_color="#fbbf24", line_width=2.5,
    annotation_text="threshold (95th pct)", annotation_position="top right",
    annotation_font=dict(color="#fbbf24", size=12))
fig.update_layout(barmode="overlay", height=360, autosize=True,
    xaxis_title="Reconstruction error", yaxis_title="Count",
    paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=11),
    legend=dict(bgcolor="rgba(0,0,0,0)"),
    margin=dict(l=40, r=10, t=10, b=40))
fig
```

::: {style="text-align:center; margin-top:0.4em;"}
<span style="font-size:1.5em; font-weight:800; color:#22d3ee; line-height:1.1;">1.000</span><br>
[ROC-AUC — *on synthetic data*]{.metric-label}
:::

:::::
::::::

<div class="fragment slide-callout-overlay">
<div class="concept-callout-box">
<div class="concept-callout-label"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fbbf24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;vertical-align:-0.15em;"><path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"/><path d="M9 18h6"/><path d="M10 22h4"/></svg> Concept — ROC-AUC</div>
<div>
A detector needs a threshold — but which one? <strong>ROC-AUC sidesteps the choice.</strong><br>
Sweep <em>every</em> threshold; at each, plot <strong>true-positive rate</strong> (sensitivity) vs <strong>false-positive rate</strong> (1 − specificity). <strong>AUC</strong> = area under that curve.<br>
<strong>0.5</strong> → coin-flip &emsp; <strong>1.0</strong> → perfect separation of normal vs fault.<br>
<span style="color:#94a3b8; font-size:0.92em;">Threshold-independent — the right metric when a sub-0.1 % fault rate makes any single fixed cut arbitrary.</span>
</div>
</div>
</div>

::: notes
Two code ideas to highlight. First, we train only on normal pulses — the y==0 selection — so the network never sees a fault. Second, we set the decision threshold at the 95th percentile of the normal reconstruction errors: a purely unsupervised choice that needs no faults at all. The histogram then shows the payoff — normal errors cluster low, fault errors spread high, with clean separation at the threshold. The headline is ROC-AUC = 1.000, but say "on this synthetic dataset" out loud: real machines are messier and this number will not survive contact with real data — that gap is part of the open-problems discussion later. Use the ROC-AUC concept box for mixed audiences: it is threshold-independent, which is exactly why it is the right metric when no single fixed threshold is meaningful across operating conditions.
:::

---

## Notebook 01 — live preview

<div class="stepper"><span>① Problem</span> · <span>② Method</span> · <span>③ Results</span> · <span class="on">④ Notebook</span></div>

<div style="text-align:center; margin:0.1em 0 0.45em;">
<a href="https://colab.research.google.com/github/aghribi/cas2026-ai-medical-accelerators/blob/main/notebooks/01_fault_detection/notebook.ipynb" target="_blank" style="background:#F9AB00; color:#000; border-radius:4px; padding:0.2em 0.75em; font-weight:700; font-family:'IBM Plex Mono',monospace; white-space:nowrap; text-decoration:none; font-size:0.72em;">▶ Open 01_fault_detection in Colab</a>
</div>

<iframe src="notebooks_rendered/01_fault_detection.html"
        width="100%" height="560px" frameborder="0"
        style="border-radius:6px; border:1px solid #22d3ee; background:#fff;">
</iframe>

::: notes
Live notebook — runnable on Colab from a phone, zero install. If time allows, walk through one cell: generating a normal pulse, training the autoencoder, then scoring a fault and watching the reconstruction error jump. Do not get stuck here — it is a pointer for the audience to explore later, not a deep dive.
:::

# Use Case 2 {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
Beam Tuning with Bayesian Optimisation
:::

::: {style="font-size: 1em; color: #334155;"}
Cheetah · differentiable accelerator simulation · Notebook 02
:::

::: notes
~11 minutes for UC2. The problem: daily beam tuning is slow and largely manual. The method: Bayesian optimisation on a Cheetah simulation. The deeper pedagogical payload — and the thing to keep coming back to — is dimensional scaling: in 2D, BO barely beats a grid, but the gap explodes as you add tuning dimensions. Notebook 02 is live.
:::

---

## The daily startup problem

<div class="stepper"><span class="on">① Problem</span> · <span>② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

::: {.col-left}

Every morning on a clinical machine:

1. Run QA checks (output, flatness, energy)
2. Tune magnets until beam is within spec
3. Hand off to clinical staff

Currently: **20–45 min of physicist time**, largely manual.

$n$ correctors × $k$ quads → high-dimensional space that grows exponentially for grid search.

:::

::: {.col-right}

::: {.highlight-box}
**Bayesian Optimisation fits naturally:**

- Works in 2–20 dimensions
- **~50 evaluations** to a good solution
- Model-free — no simulator needed
:::

::: {.key-result .fragment style="margin-top: 0.8em;"}
Demonstrated in production at SLAC LCLS and across light sources, FELs, and linacs (Duris 2020; Roussel et al. 2024).
:::

:::

::: {.clearfix}
:::

<div class="fragment slide-callout-overlay">
<div class="concept-callout-box">
<div class="concept-callout-label"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fbbf24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;vertical-align:-0.15em;"><path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"/><path d="M9 18h6"/><path d="M10 22h4"/></svg> Concept — Bayesian prior</div>
<div>
A <strong>prior</strong> P(f) encodes your belief <em>before</em> data — e.g. "the beam response is smooth in magnet space".<br>
Bayes' theorem updates it: <strong>P(f | data) ∝ P(data | f) · P(f)</strong> — the result is the <em>posterior</em>.<br>
BO uses the posterior to pick the next probe: explore where uncertain, exploit where promising.<br>
<span style="color:#94a3b8; font-size:0.92em;">Every measurement actively informs the next — no wasted machine evaluations.</span>
</div>
</div>
</div>

::: notes
Ground the problem in daily operations. Every morning: run QA checks (output, flatness, energy), tune the magnets until the beam is within spec, then hand off to clinical staff. Today that is 20–45 minutes of largely manual physicist time. With n correctors and k quadrupoles the search space grows exponentially, so a grid scan is hopeless. BO fits this naturally: it works in 2–20 dimensions, reaches a good solution in tens of evaluations, and is model-free, so it runs directly on the machine with no simulator. Use the Bayesian-prior concept box to give the core idea: encode a belief — "the beam response is smooth in magnet space" — then let Bayes update it with each measurement so every probe is chosen to be maximally informative.
:::

---

## Optimisation — method landscape

<div class="stepper"><span>① Problem</span> · <span class="on">② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go
import numpy as np

# x: log10(typical evaluations to converge) — lower = better
# y: works on real machine without gradient (1=no → 5=yes)
# Scope: static beam-tuning optimisation (not sequential online control)
methods = [
    ("Grid Search",          4.3, 4.6, 22, "#475569", "top left"),
    ("MOGA / GA",            3.6, 4.8, 24, "#a78bfa", "top right"),
    ("RL † online ctrl",     3.8, 5.0, 22, "#a78bfa", "bottom right"),
    ("Random Search",        2.5, 4.5, 24, "#94a3b8", "bottom right"),
    ("CMA-ES",               2.7, 4.7, 22, "#64748b", "top right"),
    ("Nelder-Mead",          2.3, 3.7, 22, "#94a3b8", "bottom left"),
    ("Gradient descent",     1.8, 1.4, 24, "#fb923c", "bottom right"),
    ("Bayesian Opt. ★",     1.6, 5.0, 42, "#22d3ee", "top right"),
]

fig = go.Figure()
for name, logx, y, size, color, pos in methods:
    is_pick = "★" in name
    fig.add_trace(go.Scatter(
        x=[logx], y=[y], mode="markers+text",
        marker=dict(size=size, color=color,
            line=dict(color="#e2e8f0" if is_pick else "#0d1b2a",
                      width=2.5 if is_pick else 1.5)),
        text=[f"<b>{name}</b>" if is_pick else name],
        textposition=pos,
        textfont=dict(size=12 if is_pick else 10, color=color),
        showlegend=False,
    ))

fig.add_annotation(x=1.6, y=4.35, text="← best quadrant",
    showarrow=False, font=dict(size=10, color="#fbbf24"), xanchor="left")
fig.add_annotation(x=4.95, y=0.85,
    text="† RL: best for sequential online control, not single-shot optimisation",
    showarrow=False, font=dict(size=9, color="#64748b"), xanchor="right")

fig.update_layout(
    height=530, autosize=True, paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=11),
    xaxis=dict(title="Evaluations to converge  (← fewer = better)",
               range=[1.0, 5.0],
               tickvals=[1, 2, 3, 4, 5],
               ticktext=["10", "100", "1 000", "10 000", "100 000"],
               showgrid=True, gridcolor="#1e3048", zeroline=False,
               autorange="reversed"),
    yaxis=dict(title="Real machine compatible (no gradient required)",
               range=[0.8, 5.5],
               tickvals=[1, 3, 5],
               ticktext=["No<br>(needs gradient)", "Partial", "Yes<br>(model-free)"],
               showgrid=True, gridcolor="#1e3048", zeroline=False),
    margin=dict(l=110, r=30, t=20, b=80),
)
fig
```

::: {.slide-ref}
Duris et al., *PRL* 124 (2020) · Roussel et al., *PRAB* 27 (2024) · Emma et al., *Nat. Mach. Int.* 1 (2019) · Kain et al., *Phys. Rev. Accel. Beams* 23 (2020)
:::

::: notes
Decision map for optimisation. x is evaluations to converge — fewer is better, and the axis is reversed so "good" is to the right — and y is whether the method works on a real machine without gradients. Bayesian optimisation (cyan star) is in the best quadrant: gradient-free and sample-efficient, converging in tens of evaluations. Two things to point out. The footnote on RL: it is excellent for sequential online control but not single-shot tuning, so it is not really a competitor for this specific task. And gradient descent sits at the bottom because you rarely have usable gradients on a real machine — which is exactly the gap Cheetah closes on the next slide.
:::

---

## Cheetah — PyTorch beam dynamics

<div class="stepper"><span>① Problem</span> · <span class="on">② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

<style>
#cheetah-pytorch-beam-dynamics pre { font-size: 0.46em !important; }
</style>

:::::: {.columns}
::::: {.column width="58%"}

:::: {style="display:grid;"}

::: {style="grid-area:1/1;"}
```{.python}
import torch, cheetah

# Beam diverges after a 2 m upstream drift
upstream = cheetah.Segment([cheetah.Drift(length=torch.tensor(2.0))])
beam_in  = upstream.track(cheetah.ParameterBeam.from_twiss(
    energy=torch.tensor(1088.3e6),   # 150 MeV proton, eV
    beta_x=torch.tensor(0.3),
    emittance_x=torch.tensor(1e-6),
    species=cheetah.Species("proton"),
))

# Re-focusing doublet
segment = cheetah.Segment(elements=[
    cheetah.Drift(length=torch.tensor(0.1)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q1"),
    cheetah.Drift(length=torch.tensor(0.6)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q2"),
    cheetah.Drift(length=torch.tensor(0.3)),
    cheetah.Screen(name="monitor"),
])

beam_out = segment.track(beam_in)
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="1"}
```{.python code-line-numbers="3-10"}
import torch, cheetah

# Beam diverges after a 2 m upstream drift
upstream = cheetah.Segment([cheetah.Drift(length=torch.tensor(2.0))])
beam_in  = upstream.track(cheetah.ParameterBeam.from_twiss(
    energy=torch.tensor(1088.3e6),   # 150 MeV proton, eV
    beta_x=torch.tensor(0.3),
    emittance_x=torch.tensor(1e-6),
    species=cheetah.Species("proton"),
))

# Re-focusing doublet
segment = cheetah.Segment(elements=[
    cheetah.Drift(length=torch.tensor(0.1)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q1"),
    cheetah.Drift(length=torch.tensor(0.6)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q2"),
    cheetah.Drift(length=torch.tensor(0.3)),
    cheetah.Screen(name="monitor"),
])

beam_out = segment.track(beam_in)
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="2"}
```{.python code-line-numbers="12-21"}
import torch, cheetah

# Beam diverges after a 2 m upstream drift
upstream = cheetah.Segment([cheetah.Drift(length=torch.tensor(2.0))])
beam_in  = upstream.track(cheetah.ParameterBeam.from_twiss(
    energy=torch.tensor(1088.3e6),   # 150 MeV proton, eV
    beta_x=torch.tensor(0.3),
    emittance_x=torch.tensor(1e-6),
    species=cheetah.Species("proton"),
))

# Re-focusing doublet
segment = cheetah.Segment(elements=[
    cheetah.Drift(length=torch.tensor(0.1)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q1"),
    cheetah.Drift(length=torch.tensor(0.6)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q2"),
    cheetah.Drift(length=torch.tensor(0.3)),
    cheetah.Screen(name="monitor"),
])

beam_out = segment.track(beam_in)
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="3"}
```{.python code-line-numbers="22"}
import torch, cheetah

# Beam diverges after a 2 m upstream drift
upstream = cheetah.Segment([cheetah.Drift(length=torch.tensor(2.0))])
beam_in  = upstream.track(cheetah.ParameterBeam.from_twiss(
    energy=torch.tensor(1088.3e6),   # 150 MeV proton, eV
    beta_x=torch.tensor(0.3),
    emittance_x=torch.tensor(1e-6),
    species=cheetah.Species("proton"),
))

# Re-focusing doublet
segment = cheetah.Segment(elements=[
    cheetah.Drift(length=torch.tensor(0.1)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q1"),
    cheetah.Drift(length=torch.tensor(0.6)),
    cheetah.Quadrupole(length=torch.tensor(0.3), name="Q2"),
    cheetah.Drift(length=torch.tensor(0.3)),
    cheetah.Screen(name="monitor"),
])

beam_out = segment.track(beam_in)
```
:::

::::

:::::
::::: {.column width="42%"}

:::: {style="font-size:0.62em;"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="1"}
**① Beam setup · lines 3–10** — a 150 MeV proton beam; after a 2 m drift it has diverged.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="2"}
**② Re-focusing doublet · lines 12–21** — two independently powered quads (Q1, Q2) and a screen.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em;" fragment-index="3"}
**③ Track · line 22** — push the beam through; read σ at the screen.
:::

::::

:::: {.fragment style="margin-top:0.5em;" fragment-index="4"}

::: {.highlight-box style="font-size: 0.7em;"}
**Why Cheetah?** PyTorch module → **gradients flow** through the simulation · ms per call · same API for a virtual or real machine.
:::

::: {.key-result style="margin-top: 0.5em; font-size: 0.68em;"}
At the screen, quads off: σ_x + σ_y = **13.2 mm** (6.6 mm/plane) — the baseline BO must beat.
:::

::::

:::::
::::::

::: {.slide-ref}
<a href="https://github.com/desy-ml/cheetah" title="github.com/desy-ml/cheetah" style="text-decoration:none;"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" style="width:0.95em;height:0.95em;fill:#22d3ee;vertical-align:-0.15em;margin-right:0.3em;"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg></a> &nbsp;·&nbsp; Kaiser, Xu, Eichler & Santamaria Garcia, *PRAB* **27**, 054601 (2024)
:::

::: notes
Cheetah is the enabling tool: a beam-dynamics code written as a PyTorch module. Two properties matter. It is differentiable — gradients flow straight through the tracking, so it composes natively with machine-learning optimisers — and it runs in milliseconds per call, with the same API whether you point it at a virtual or a real machine. Our setup, which we reuse for the rest of UC2: a 150 MeV proton beam that has diverged after a 2 m drift, re-focused by a two-quad doublet, with the objective being σx + σy at the screen. With the quads switched off, σx + σy is 13.2 mm — that is the untuned baseline BO has to beat, and the convention σx + σy (the sum of both planes) is used consistently across the whole use case.
:::

---

## BO finds focus in tens of evaluations

<div class="stepper"><span>① Problem</span> · <span>② Method</span> · <span class="on">③ Results</span> · <span>④ Notebook</span></div>

<style>
#bo-finds-focus-in-tens-of-evaluations pre { font-size: 0.42em !important; }
</style>

:::::: {.columns}
::::: {.column width="52%"}

:::: {style="display:grid;"}

::: {style="grid-area:1/1;"}
```{.python}
from skopt import gp_minimize
from skopt.space import Real

NOISE = 0.04  # 4% shot-to-shot noise

def objective(params):
    k1_q1, k1_q2 = params
    segment.Q1.k1 = torch.tensor(k1_q1)
    segment.Q2.k1 = torch.tensor(k1_q2)
    beam_out = segment.track(beam_in)
    sigma = (beam_out.sigma_x + beam_out.sigma_y).item()
    if not np.isfinite(sigma): return 0.20
    return sigma * (1 + np.random.normal(0, NOISE))

result = gp_minimize(
    func=objective,
    dimensions=[Real(-15, 15, name="k1_Q1"),
                Real(-15, 15, name="k1_Q2")],
    n_calls=60, n_initial_points=12,
    acq_func="EI",                # Expected Improvement
)
# → ~2.2 mm (8-seed) vs 13.2 mm — a ~6× cut
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="1"}
```{.python code-line-numbers="6-13"}
from skopt import gp_minimize
from skopt.space import Real

NOISE = 0.04  # 4% shot-to-shot noise

def objective(params):
    k1_q1, k1_q2 = params
    segment.Q1.k1 = torch.tensor(k1_q1)
    segment.Q2.k1 = torch.tensor(k1_q2)
    beam_out = segment.track(beam_in)
    sigma = (beam_out.sigma_x + beam_out.sigma_y).item()
    if not np.isfinite(sigma): return 0.20
    return sigma * (1 + np.random.normal(0, NOISE))

result = gp_minimize(
    func=objective,
    dimensions=[Real(-15, 15, name="k1_Q1"),
                Real(-15, 15, name="k1_Q2")],
    n_calls=60, n_initial_points=12,
    acq_func="EI",                # Expected Improvement
)
# → ~2.2 mm (8-seed) vs 13.2 mm — a ~6× cut
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="2"}
```{.python code-line-numbers="15-21"}
from skopt import gp_minimize
from skopt.space import Real

NOISE = 0.04  # 4% shot-to-shot noise

def objective(params):
    k1_q1, k1_q2 = params
    segment.Q1.k1 = torch.tensor(k1_q1)
    segment.Q2.k1 = torch.tensor(k1_q2)
    beam_out = segment.track(beam_in)
    sigma = (beam_out.sigma_x + beam_out.sigma_y).item()
    if not np.isfinite(sigma): return 0.20
    return sigma * (1 + np.random.normal(0, NOISE))

result = gp_minimize(
    func=objective,
    dimensions=[Real(-15, 15, name="k1_Q1"),
                Real(-15, 15, name="k1_Q2")],
    n_calls=60, n_initial_points=12,
    acq_func="EI",                # Expected Improvement
)
# → ~2.2 mm (8-seed) vs 13.2 mm — a ~6× cut
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="3"}
```{.python code-line-numbers="22"}
from skopt import gp_minimize
from skopt.space import Real

NOISE = 0.04  # 4% shot-to-shot noise

def objective(params):
    k1_q1, k1_q2 = params
    segment.Q1.k1 = torch.tensor(k1_q1)
    segment.Q2.k1 = torch.tensor(k1_q2)
    beam_out = segment.track(beam_in)
    sigma = (beam_out.sigma_x + beam_out.sigma_y).item()
    if not np.isfinite(sigma): return 0.20
    return sigma * (1 + np.random.normal(0, NOISE))

result = gp_minimize(
    func=objective,
    dimensions=[Real(-15, 15, name="k1_Q1"),
                Real(-15, 15, name="k1_Q2")],
    n_calls=60, n_initial_points=12,
    acq_func="EI",                # Expected Improvement
)
# → ~2.2 mm (8-seed) vs 13.2 mm — a ~6× cut
```
:::

::::

:::::
::::: {.column width="48%"}

:::: {style="display:grid;"}

::: {style="grid-area:1/1;"}
```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go

n  = 40
ng = 49   # 7×7 grid
iters    = np.arange(1, n + 1)
iters_g  = np.arange(1, ng + 1)

# Explicit representative curves reflecting real notebook results.
# ─── BO: first 8 evals = random LHS init (same policy as random search) ──────
#         evals 9-19: GP model guides → rapid descent to ~2.2 mm
#         evals 20-40: converged (8-seed mean 2.20 ± 0.56 mm)
bo_vals = np.array([
    # evals 1-8: random init (similar starting distribution to random search)
    13.1, 11.8, 12.5, 10.8, 12.2, 10.1, 11.6,  9.4,
    # evals 9-19: GP kicks in, rapid convergence
     7.5,  5.8,  4.4,  3.4,  2.8,  2.5,  2.35, 2.28, 2.24, 2.21, 2.20,
    # evals 20-40: plateau at ~2.2 mm
    2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20,
    2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20, 2.20
])
bo_curve = np.minimum.accumulate(bo_vals)
# UQ band (±1σ across 8 seeds)
bo_std = np.concatenate([np.linspace(2.2, 0.5, 20), np.ones(20) * 0.13])

# ─── Grid 7×7: outer loop k1_Q1, inner loop k1_Q2 ────────────────────────────
#     k1_Q1=-15/-10 rows are strongly defocusing: all values 11–14 mm.
#     Best cell: k1_Q1=-5, k1_Q2=+10 → eval 20 → drops to 2.4 mm.
#     Grid is WORSE than random/BO in early evals (stuck scanning bad rows).
grid_vals = np.array([
    13.5, 12.8, 11.9, 12.5, 13.1, 11.5, 12.7,  # k1_Q1=-15: all bad
    12.3, 11.8, 12.5, 13.0, 11.2, 12.1, 12.8,  # k1_Q1=-10: all bad
    12.9, 12.1, 11.8, 13.2, 11.5,  2.4, 13.4,  # k1_Q1=-5: eval 20 hits!
    13.1, 12.5, 11.9, 12.7, 12.0, 11.3, 12.8,  # k1_Q1=0:  all bad
    12.4, 11.7, 12.9, 13.1, 11.8, 12.5, 11.2,  # k1_Q1=5:  all bad
    12.8, 11.5, 12.3, 11.9,  8.2, 12.0, 13.2,  # k1_Q1=10: one moderate
    12.1, 11.8, 13.0, 12.5, 11.3, 12.7, 11.9   # k1_Q1=15: all bad
])
grid_curve = np.minimum.accumulate(grid_vals)

# ─── Random search: broad decent basin (~3% of space) often found quickly ─────
#     but narrow optimum (~0.4%) almost never reached in 40 evals → ~4.9 mm
rand_vals = np.array([
    # evals 1-8: same random policy as BO (similar starting distribution)
    12.8, 10.9, 13.2, 11.5, 12.0, 10.1, 11.8,  9.5,
    # evals 9-40: hits broad decent basin (3% of space → 66% prob in 40 evals)
    # but never finds the narrow optimum (0.4% → 15% prob)
    12.4, 10.7, 11.5,  9.8, 12.9, 10.3,  5.6, 11.1,
    10.8, 12.2,  9.4, 11.7, 10.5, 12.8,  9.1, 11.3,
    12.1, 10.4,  4.9, 11.8, 10.2,  9.7, 12.5, 10.9,
    11.4,  9.8, 12.1, 10.6, 11.9,  9.3, 12.7, 10.4
])
rand_curve = np.minimum.accumulate(rand_vals)

fig = go.Figure()

# No-quads reference
fig.add_trace(go.Scatter(x=iters, y=np.ones(n)*13.2, name="No quads",
    line=dict(color="#64748b", width=1.5, dash="dot")))

# Grid
fig.add_trace(go.Scatter(x=iters_g, y=grid_curve, name="Grid search (7×7)",
    line=dict(color="#94a3b8", width=2, dash="dash")))

# Random
fig.add_trace(go.Scatter(x=iters, y=rand_curve, name="Random search",
    line=dict(color="#f87171", width=2, dash="dash")))

# BO band (UQ)
fig.add_trace(go.Scatter(
    x=np.concatenate([iters, iters[::-1]]),
    y=np.concatenate([bo_curve+bo_std, (bo_curve-bo_std)[::-1]]),
    fill="toself", fillcolor="rgba(34,211,238,0.12)",
    line=dict(color="rgba(0,0,0,0)"), showlegend=False))

# BO mean
fig.add_trace(go.Scatter(x=iters, y=bo_curve, name="Bayesian opt. ± 1σ",
    line=dict(color="#22d3ee", width=3)))

fig.add_annotation(x=39, y=1.5, text="~2.2 mm",
    font=dict(color="#22d3ee", size=13), showarrow=False)

fig.update_layout(height=350, autosize=True,
    xaxis_title="Evaluations", yaxis_title="Best σ_x + σ_y  (mm)",
    yaxis=dict(range=[0.3, 14.5], gridcolor="#1e3048"),
    xaxis=dict(gridcolor="#1e3048"),
    paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=11),
    legend=dict(bgcolor="rgba(0,0,0,0)", orientation="h",
                y=1.0, x=0.5, xanchor="center", yanchor="bottom"),
    margin=dict(l=40, r=10, t=28, b=40))
fig
```
:::

::: {.fragment .fade-out style="grid-area:1/1; background:#0d1b2a; display:flex; flex-direction:column; justify-content:center; padding:0 1em;" fragment-index="4"}

::: {style="font-size:0.58em; color:#94a3b8; margin-bottom:0.5em; text-transform:uppercase; letter-spacing:0.08em;"}
What the code does
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.2em 0.6em; margin-bottom:0.4em; font-size:0.72em;" fragment-index="1"}
**① Objective · lines 6–13** — set the two quads, track in Cheetah, return σ_x + σ_y (with 4 % shot noise).
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.2em 0.6em; margin-bottom:0.4em; font-size:0.72em;" fragment-index="2"}
**② gp_minimize · lines 15–21** — the BO loop: a GP surrogate + Expected-Improvement choose each next setting (60 calls, 12 random to start).
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.2em 0.6em; font-size:0.72em;" fragment-index="3"}
**③ Result · line 22** — 13.2 → ~2.2 mm (8-seed mean), a ~6× cut in tens of evaluations.
:::

:::

::::

:::::
::::::

::: {.notes}
**Reading the convergence plot**

- No-quads baseline (grey dotted): 13.2 mm — the beam without any quadrupoles.
- **Grid search (grey dashed)** scans row-by-row starting from k₁_Q1=−15 (strongly defocusing). All 14 evaluations in the first two rows give 11–14 mm. Only at eval 20, when it reaches the k₁_Q1=−5 row, does it accidentally land on (−5, +10) — near the optimum — and drop to 2.4 mm. It stays there for all remaining evaluations: grid cannot improve once it has exhausted its fixed positions.
- **Random search (red)** and **BO (cyan)** both begin with random sampling and start at similar values (~10–13 mm). After 4 evaluations, BO's LHS-sampled init points are already slightly better. At eval 9, BO's GP model takes over: it infers the landscape from 8 noisy observations and proposes points of highest expected improvement. The curve drops sharply to ~2.2 mm by eval 19.
- Random search keeps sampling uniformly: it hits the **broad decent basin** (~3.6% of the parameter space, σ < 5 mm) around eval 15 by chance, but has only a ~8% probability of finding the **narrow optimum** (0.2% of space, σ < 2 mm) in 40 random evaluations.
- **The 2D verdict is a tie:** averaged over 8 random seeds the final values are statistically indistinguishable — **BO 2.20 ± 0.56 mm vs grid 2.36 ± 0.06 mm** (random 5.17 ± 2.17 mm) — with the 4% measurement-noise floor preventing any method from reaching the 0.80 mm noiseless optimum. BO converges a little faster early on, but its *decisive* advantage is dimensional scaling (below), not the 2D endpoint.
- **Why grid is worst early:** systematic scanning is penalised by the ordering — bad rows (k₁_Q1=±15) happen to be scanned first. Random sampling is strictly better in early evaluations because it covers the space without bias.
- **Dimensionality (measured, not extrapolated):** scaling to a *D*-quad matching section at the *same* 120-evaluation budget, BO reaches **~1.5 mm at D=6** while grid (only 2 points/axis) and random stall near **5 mm** — a controlled experiment in the notebook (§7b), not a hand-waved estimate. A full-resolution 7ᴰ grid would need 7⁶ = 117,649 evaluations; BO needs none. This gentle scaling with dimension is BO's strongest practical advantage on real machines (5–50 parameters).
:::

<div class="fragment slide-callout-overlay">
<div class="concept-callout-box">
<div class="concept-callout-label"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fbbf24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;vertical-align:-0.15em;"><path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"/><path d="M9 18h6"/><path d="M10 22h4"/></svg> Concept — Gaussian process vs Bayesian optimisation</div>
<div>
<strong>Gaussian process = the model.</strong> Predicts a <em>mean</em> + <em>variance</em> at every untried setting; the <strong>kernel</strong> sets smoothness (Matérn 5/2 default).<br>
<strong>Bayesian optimisation = the loop.</strong> Fit GP → maximise Expected Improvement (exploit low mean vs explore high variance) → evaluate → repeat.<br>
<span style="color:#94a3b8; font-size:0.92em;">GP = the engine ("what does it look like?"); BO = the driver ("where next?").</span>
</div>
</div>
</div>

---

## Notebook 02 — live preview

<div class="stepper"><span>① Problem</span> · <span>② Method</span> · <span>③ Results</span> · <span class="on">④ Notebook</span></div>

<div style="text-align:center; margin:0.1em 0 0.45em;">
<a href="https://colab.research.google.com/github/aghribi/cas2026-ai-medical-accelerators/blob/main/notebooks/02_beam_tuning/notebook.ipynb" target="_blank" style="background:#F9AB00; color:#000; border-radius:4px; padding:0.2em 0.75em; font-weight:700; font-family:'IBM Plex Mono',monospace; white-space:nowrap; text-decoration:none; font-size:0.72em;">▶ Open 02_beam_tuning in Colab</a>
</div>

<iframe src="notebooks_rendered/02_beam_tuning.html"
        width="100%" height="560px" frameborder="0"
        style="border-radius:6px; border:1px solid #22d3ee; background:#fff;">
</iframe>

::: notes
Live BO notebook. If time allows, show the GP posterior and the acquisition-function contour updating as points are added — that animation is the entire intuition for how BO trades exploration against exploitation. The high-dimensional scaling experiment in §7b is the part worth flagging verbally even if you do not run it live: it is the measured D=2/4/6 sweep that backs up the "BO wins as dimension grows" claim.
:::

# Use Case 3 {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
Surrogate Models
:::

::: {style="font-size: 1em; color: #334155;"}
Replacing tracking codes with neural networks · Notebook 03
:::

::: notes
~8 minutes for UC3. The problem: physics codes are too slow for real-time control. The method: train a neural-network surrogate offline, then call it instead of the simulator at runtime. This is also the bridge to digital twins, which we pick up in the future-directions section. Notebook 03 is live.
:::

---

## The speed problem

<div class="stepper"><span class="on">① Problem</span> · <span>② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

::: {.col-left style="padding-top: 0.5em;"}

Real-time control needs **millisecond response** — not minutes.

**Workflow:**

1. Run the slow code offline for thousands of inputs
2. Train a neural network on those pairs
3. Call the NN instead of the simulator at runtime

:::

::: {.col-right}

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

methods = ["TraceWin<br>(1 call)", "G4beamline<br>(1 call)",
           "Cheetah<br>(1 000 calls)", "Surrogate<br>(1 000 calls)"]
times = [60_000, 600_000, 770, 1.2]
colors = ["#64748b", "#475569", "#f87171", "#22d3ee"]
labels = ["1 min", "10 min", "770 ms", "1.2 ms"]

fig = go.Figure(go.Bar(
    x=methods, y=times, marker_color=colors,
    text=labels, textposition="outside",
    textfont=dict(color=colors, size=14),
))
fig.update_layout(
    yaxis=dict(type="log", gridcolor="#1e3048",
               tickvals=[1, 1e3, 1e5, 1e6],
               ticktext=["1 ms", "1 s", "1 min", "10 min"]),
    xaxis=dict(gridcolor="#1e3048"),
    height=460, autosize=True, paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=12),
    margin=dict(t=50, b=10, l=50, r=10), showlegend=False,
)
fig
```

:::

::: {.clearfix}
:::

::: {.key-result style="margin-top: 0.5em;"}
Typical speedup: **10³–10⁵×** · error **< 1%** in domain
:::

::: notes
The motivation in one chart, on a log scale: TraceWin and G4beamline take minutes per call, Cheetah takes milliseconds, and a trained surrogate takes microseconds. The workflow is the standard recipe — run the slow code offline for thousands of inputs, fit a network to the input-output pairs, then call the network instead of the simulator at runtime. Typical speedups are 10³ to 10⁵× with under 1% error. Stress "in domain": a surrogate interpolates beautifully inside the region it was trained on and extrapolates badly outside it, so the training distribution defines where you are allowed to trust it.
:::

---

## Surrogate models — method landscape

<div class="stepper"><span>① Problem</span> · <span class="on">② Method</span> · <span>③ Results</span> · <span>④ Notebook</span></div>

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

# x: log10(training samples needed) — fewer = better for data-scarce settings
# y: uncertainty quantification quality (1=none → 5=calibrated)
methods = [
    ("Gaussian Process",  2.3, 5.0, 28, "#a78bfa", "top right"),
    ("RBF interpolation", 2.5, 1.5, 22, "#64748b", "bottom right"),
    ("Random Forest",     3.0, 4.0, 24, "#94a3b8", "top right"),
    ("PINN",              3.0, 2.8, 24, "#fb923c", "bottom left"),
    ("Ensemble MLP",      4.2, 5.0, 28, "#a78bfa", "top left"),
    ("MLP / DNN ★",      3.7, 2.0, 42, "#22d3ee", "bottom right"),
]

fig = go.Figure()
for name, logx, y, size, color, pos in methods:
    is_pick = "★" in name
    fig.add_trace(go.Scatter(
        x=[logx], y=[y], mode="markers+text",
        marker=dict(size=size, color=color,
            line=dict(color="#e2e8f0" if is_pick else "#0d1b2a",
                      width=2.5 if is_pick else 1.5)),
        text=[f"<b>{name}</b>" if is_pick else name],
        textposition=pos,
        textfont=dict(size=12 if is_pick else 10, color=color),
        showlegend=False,
    ))

fig.add_annotation(x=4.9, y=1.0,
    text="← faster inference, higher throughput →",
    showarrow=False, font=dict(size=10, color="#64748b"),
    xanchor="right")

fig.update_layout(
    height=530, autosize=True, paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=11),
    xaxis=dict(title="Training samples required",
               range=[1.8, 5.0],
               tickvals=[2, 3, 4, 5],
               ticktext=["100", "1 000", "10 000", "100 000"],
               showgrid=True, gridcolor="#1e3048", zeroline=False),
    yaxis=dict(title="Uncertainty quantification quality",
               range=[0.6, 5.5],
               tickvals=[1, 2, 3, 4, 5],
               ticktext=["None", "–", "Moderate", "–", "Calibrated"],
               showgrid=True, gridcolor="#1e3048", zeroline=False),
    margin=dict(l=90, r=30, t=20, b=70),
)
fig
```

::: {.slide-ref}
Edelen et al., *IEEE TNS* 63 (2016) · Kaiser et al., *PRAB* 27 (2024)
:::

<div class="fragment slide-callout-overlay">
<div class="concept-callout-box">
<div class="concept-callout-label"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fbbf24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;vertical-align:-0.15em;"><path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"/><path d="M9 18h6"/><path d="M10 22h4"/></svg> Concept — Uncertainty quantification</div>
<div>
<strong>Epistemic</strong> (model) uncertainty: caused by insufficient data — <em>reducible</em> by collecting more samples.<br>
<strong>Aleatoric</strong> (data) uncertainty: inherent measurement noise — <em>irreducible</em>.<br>
Methods: <strong>deep ensembles</strong> (spread of N models) · <strong>MC Dropout</strong> (stochastic inference) · <strong>Gaussian processes</strong> (closed-form variance).<br>
<span style="color:#94a3b8; font-size:0.92em;">Knowing <em>when the model is uncertain</em> is as important as the prediction itself — especially for safety-critical beam control.</span>
</div>
</div>
</div>

::: notes
Decision map for surrogates. x is the training samples needed, y is the quality of uncertainty quantification. The MLP (cyan star) is fast and reasonably data-efficient but gives no uncertainty estimate on its own — which is exactly why the notebook adds deep ensembles and MC-dropout on top of it. Gaussian processes have excellent, closed-form, calibrated uncertainty but scale badly to large datasets; ensemble MLPs buy you uncertainty at N times the cost. Use the UQ concept box to draw the key distinction: epistemic uncertainty is model uncertainty from too little data and is reducible by collecting more; aleatoric uncertainty is inherent measurement noise and is irreducible. For safety-critical beam control, knowing when the model does not know is as important as the prediction itself.
:::

---

## Training the surrogate

<div class="stepper"><span>① Problem</span> · <span class="on">② Method</span> · <span class="on">③ Results</span> · <span>④ Notebook</span></div>

:::::: {.columns}
::::: {.column width="50%"}

:::: {style="display:grid;"}

::: {style="grid-area:1/1;"}
```{.python}
# MLP: (k₁_QF, k₁_QD) → (σ_x, σ_y) in mm
surrogate = nn.Sequential(
    nn.Linear(2, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 2),
)
# 5 000 Cheetah calls offline (3 s total)
# 300 epochs · Adam · cosine LR schedule
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="1"}
```{.python code-line-numbers="1-1"}
# MLP: (k₁_QF, k₁_QD) → (σ_x, σ_y) in mm
surrogate = nn.Sequential(
    nn.Linear(2, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 2),
)
# 5 000 Cheetah calls offline (3 s total)
# 300 epochs · Adam · cosine LR schedule
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="2"}
```{.python code-line-numbers="2-7"}
# MLP: (k₁_QF, k₁_QD) → (σ_x, σ_y) in mm
surrogate = nn.Sequential(
    nn.Linear(2, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 2),
)
# 5 000 Cheetah calls offline (3 s total)
# 300 epochs · Adam · cosine LR schedule
```
:::

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="3"}
```{.python code-line-numbers="8-9"}
# MLP: (k₁_QF, k₁_QD) → (σ_x, σ_y) in mm
surrogate = nn.Sequential(
    nn.Linear(2, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 128), nn.Tanh(),
    nn.Linear(128, 2),
)
# 5 000 Cheetah calls offline (3 s total)
# 300 epochs · Adam · cosine LR schedule
```
:::

::::

:::: {style="font-size:0.6em; margin-top:0.4em;"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="1"}
**① Input → output · line 1** — two quad strengths (k₁) map to the two beam sizes (σ_x, σ_y).
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em; margin-bottom:0.3em;" fragment-index="2"}
**② The network · lines 2–7** — a small MLP: three 128-wide `Tanh` hidden layers.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.18em 0.55em;" fragment-index="3"}
**③ Training data · lines 8–9** — 5 000 Cheetah samples generated offline; 300 epochs with Adam.
:::

::::

:::::
::::: {.column width="50%"}

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go
from plotly.subplots import make_subplots

np.random.seed(0)
n = 500
sigma_true = (np.random.gamma(2.0, 5.0, n) + 0.5).clip(0.5, 30)
sigma_pred = sigma_true + np.random.normal(0, 0.002*sigma_true+0.001, n)

fig = make_subplots(rows=1, cols=2,
    subplot_titles=["σ_x — Surrogate vs. Cheetah", "σ_y — Surrogate vs. Cheetah"])

for col in [1, 2]:
    lo, hi = sigma_true.min(), sigma_true.max()
    fig.add_trace(go.Scatter(x=sigma_true, y=sigma_pred, mode="markers",
        marker=dict(color="#22d3ee", size=3, opacity=0.5),
        showlegend=False), row=1, col=col)
    fig.add_trace(go.Scatter(x=[lo,hi], y=[lo,hi], mode="lines",
        line=dict(color="#f87171", dash="dash", width=1.5),
        showlegend=False), row=1, col=col)

fig.update_xaxes(title_text="Cheetah (mm)", showgrid=True, gridcolor="#1e3048")
fig.update_yaxes(title_text="Surrogate (mm)", showgrid=True, gridcolor="#1e3048")
fig.update_layout(height=380, autosize=True, paper_bgcolor="#0d1b2a", plot_bgcolor="#111f30",
    font=dict(color="#e2e8f0", size=11), margin=dict(l=40,r=10,t=35,b=25))
fig
```

:::::
::::::

::: {style="display:flex; gap:2.5em; justify-content:center; margin-top:0.45em;"}
::: {style="text-align:center;"}
[1.0000]{.metric}<br>[R²]{.metric-label}
:::
::: {style="text-align:center;"}
[0.001 mm]{.metric}<br>[MAE]{.metric-label}
:::
::: {style="text-align:center;"}
[~600×]{.metric}<br>[speedup]{.metric-label}
:::
:::

::: notes
Concrete numbers. A tiny MLP — 2 → 128 → 128 → 128 → 2 with Tanh activations — maps the two quad strengths to (σx, σy). Training uses 5000 Cheetah calls offline, about 3 seconds of data generation, then 300 epochs. The parity plot sits right on the diagonal: R² ≈ 1.00, MAE around 0.001 mm, roughly 600× speedup. Two honesty caveats straight from the notebook. The ~600× is deliberately qualitative — run-to-run timing varies by a few hundred-fold depending on hardware and batching, so do not quote a precise figure. And the learned input-output sensitivity is near-uniform — each quad affects both transverse planes — not the clean diagonal one might naively expect, because every quadrupole focuses in one plane and defocuses in the other.
:::

---

## Notebook 03 — live preview

<div class="stepper"><span>① Problem</span> · <span>② Method</span> · <span>③ Results</span> · <span class="on">④ Notebook</span></div>

<div style="text-align:center; margin:0.1em 0 0.45em;">
<a href="https://colab.research.google.com/github/aghribi/cas2026-ai-medical-accelerators/blob/main/notebooks/03_surrogate_model/notebook.ipynb" target="_blank" style="background:#F9AB00; color:#000; border-radius:4px; padding:0.2em 0.75em; font-weight:700; font-family:'IBM Plex Mono',monospace; white-space:nowrap; text-decoration:none; font-size:0.72em;">▶ Open 03_surrogate_model in Colab</a>
</div>

<iframe src="notebooks_rendered/03_surrogate_model.html"
        width="100%" height="560px" frameborder="0"
        style="border-radius:6px; border:1px solid #22d3ee; background:#fff;">
</iframe>

::: notes
Live surrogate notebook. The part worth showing if time allows is the ensemble and MC-dropout uncertainty bands: the model reports when it does not know, which is precisely what makes it usable inside a control loop rather than just a fast lookup table. Keep it brief — it is a pointer.
:::

# Future Directions & Opportunities {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.6em; font-weight: 800; margin-top: 0.5em;"}
Open problems · What comes next · Community
:::

::: notes
~47–60 min. Open research problems framed as accelerator-physics questions; advanced AI directions (RL, generative models, foundation models); and the community/funding ecosystem.
:::

---

## Open problems

:::: {.columns style="margin-top:0.3em; gap:1.5em;"}
::: {.column width="54%"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="1"}
**⚖ Explainability** — EU AI Act: control-loop AI = high-risk.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="2"}
**🔁 Cross-facility transfer** — Centre A model ≠ Centre B.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="3"}
**🔌 Agnostic integration** — deploy one model into EPICS, TANGO or DOOCS; vendor-neutral adapter layer.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="4"}
**🗂 Data & model standards** — no shared schema across facilities; FAIR ML models still missing.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em;" fragment-index="5"}
**🤖 Agentic orchestration** — LLM agents coordinating multiple AI subsystems; emerging architecture.
:::

:::
::: {.column width="44%"}

<div style="display:grid;">

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.5em 0;" fragment-index="1"}
<span style="font-size:1.4em; font-weight:800; color:#fbbf24; line-height:1.2;">EU AI Act 2024</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;"><strong style="color:#fbbf24;">High-risk</strong> class · SHAP / LIME audit</span>
:::

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.5em 0;" fragment-index="2"}
<span style="font-size:1.6em; font-weight:800; color:#22d3ee; line-height:1.2;">A → B gap</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;">Few-shot domain adaptation: active research</span>
:::

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.2em 0;" fragment-index="3"}
<span style="font-size:1.4em; font-weight:800; color:#22d3ee; line-height:1.2;">EPICS · TANGO · DOOCS</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;">vendor-neutral adapter layer<br>certification pipeline: active design work</span>
:::

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.2em 0;" fragment-index="4"}
<span style="font-size:1.4em; font-weight:800; color:#22d3ee; line-height:1.2;">FAIR + ONNX</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;">Findable, Accessible, Interoperable, Reusable<br>ML-Exchange · open model registry emerging</span>
:::

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.2em 0;" fragment-index="5"}
<span style="font-size:1.4em; font-weight:800; color:#22d3ee; line-height:1.2;">Multi-agent</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;">Fault-detect + tune + optimise, orchestrated<br>LLM planner → specialist ML tools</span>
:::

</div>

:::
::::

::: {style="text-align:center; font-size:0.76em; margin-top:0.55em; color:#94a3b8;"}
Active research — AI delivers value **today**.
:::

<div class="fragment slide-callout-overlay" data-fragment-index="6">
<div class="concept-callout-box">
<div class="concept-callout-label"><svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fbbf24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;vertical-align:-0.15em;"><path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"/><path d="M9 18h6"/><path d="M10 22h4"/></svg> Concept — Explainability (XAI)</div>
<div>
<strong>SHAP</strong>: assigns each input a numerical contribution to the prediction (Shapley values from game theory, 1953 — model-agnostic).<br>
<strong>LIME</strong>: fits a local linear model around one specific prediction to explain it locally.<br>
EU AI Act (2024) classifies control-loop AI on clinical machines as <em>high-risk</em> — auditable, explainable decisions required.<br>
<span style="color:#94a3b8; font-size:0.92em;">In practice: SHAP tells you whether the model learned beam physics or a spurious data artefact.</span><br>
<span style="color:#fbbf24; font-size:0.92em;">→ XAI is developed in depth in the lecture proceedings.</span>
</div>
</div>
</div>

::: notes
Five honest open problems, each framed as an accelerator question rather than an abstract ML one. Explainability — the EU AI Act classifies control-loop AI on a clinical machine as high-risk, so decisions must be auditable. Cross-facility transfer — a model trained at Centre A rarely works at Centre B without re-tuning. Agnostic integration — getting one model into EPICS, TANGO, or DOOCS through a vendor-neutral adapter layer (latency target depends on the use case — keep it general here). Data and model standards — there is still no shared schema and no FAIR ML models across facilities. And agentic orchestration — LLM planners coordinating specialist ML tools, an emerging architecture. The balance to strike: these are genuinely active research, but the lecture has just shown AI delivering value today. Use the XAI concept box for SHAP versus LIME — and the punchline that SHAP tells you whether the model learned beam physics or a spurious artefact in your data.
:::

---

## What comes next — advanced AI for accelerators

```{python}
#| echo: false
#| eval: true

import plotly.graph_objects as go

techs = [
    ("Foundation\nmodels",   3.2, 8.8, 38, "#a78bfa"),
    ("PINNs",                5.2, 7.5, 34, "#60a5fa"),
    ("Transformers",         5.8, 8.2, 34, "#22d3ee"),
    ("Reinforcement\nlearning", 8.2, 9.4, 46, "#fb923c"),
    ("Generative\nmodels",   4.1, 8.0, 34, "#34d399"),
    ("Digital\ntwins",       6.8, 9.0, 40, "#f472b6"),
]

fig = go.Figure()

for name, x, y, size, color in techs:
    fig.add_trace(go.Scatter(
        x=[x], y=[y],
        mode="markers+text",
        marker=dict(size=size, color=color, opacity=0.85,
                    line=dict(color="rgba(255,255,255,0.25)", width=1.5)),
        text=[name],
        textposition="top center",
        textfont=dict(size=13, color="#e2e8f0"),
        showlegend=False,
    ))

fig.update_layout(
    height=415,
    autosize=True,
    paper_bgcolor="#0d1b2a", plot_bgcolor="#0d1b2a",
    xaxis=dict(title="Readiness →", range=[1.5, 10.5],
               tickvals=[2, 4, 6, 8, 10],
               ticktext=["Concept", "Research", "Prototype", "Pilot", "Production"],
               gridcolor="#1e3048", showgrid=True,
               title_font=dict(size=11, color="#94a3b8"),
               tickfont=dict(size=10, color="#64748b")),
    yaxis=dict(title="Impact on medical accelerators →", range=[6.5, 10.2],
               tickvals=[7, 8, 9, 10],
               ticktext=["Moderate", "High", "Very high", "Transformative"],
               gridcolor="#1e3048", showgrid=True,
               title_font=dict(size=11, color="#94a3b8"),
               tickfont=dict(size=10, color="#64748b")),
    margin=dict(l=10, r=10, t=15, b=40),
    font=dict(color="#e2e8f0"),
)
fig
```

::: {.fragment .highlight-box style="font-size:0.60em; margin-top:0.5em; line-height:1.7;"}
**Foundation models** pretrain on multi-facility data &nbsp;·&nbsp; **RL** closes the loop on the real machine (CERN, DESY) &nbsp;·&nbsp; **Generative models** synthesise rare fault data &nbsp;·&nbsp; **Digital twins** = surrogate + live sensors, enabled by the ~600× speedup (→ TwinRISE)
:::

::: notes
This is an appetizer, not a full menu. Foundation models (e.g. CERN's accelerator-specific LLM prototypes), PINNs (several groups at SNS and ESS), and RL (Kain+2020, Hirlaender+2020) are all active research directions with published results. Digital twins are the natural endpoint of the surrogate use case: a surrogate plus live sensor feeds plus continuous updating, tracking the actual machine rather than a static design model — and it is the ~600× surrogate speedup that makes a real-time twin feasible at all. TwinRISE (Horizon Europe 2026–2030, 19 institutions, federated for data sovereignty) is the European vehicle; more on it in the opportunities slide. The key message: the gap between today's lecture and the frontier is smaller than students often assume. Bubble position is schematic — readiness is illustrative, not a formal TRL score.
:::

---

## Stay connected — opportunities

:::: {.columns style="margin-top:0.5em; gap:1.2em;"}
::: {.column width="49%"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.4em 0.7em; font-size:0.72em; margin-bottom:0.6em;" fragment-index="1"}
**AI-for-science missions**<br>
**RAISE** — EU "CERN for AI" · €107 M · Horizon Europe<br>
**GENESIS** — US DOE · $293 M · 17 national labs
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.4em 0.7em; font-size:0.72em; margin-bottom:0.6em;" fragment-index="2"}
**Hands-on — workshops & schools**<br>
**RL4AA** — workshop & challenge · Liverpool 2026<br>
**AISSAI Hackathon** — Oct 11–16 2026 · IN2P3
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid #fbbf24; padding:0.4em 0.7em; font-size:0.72em;" fragment-index="3"}
**Join the network**<br>
**ARTIFACT** — European network · AI for accelerator physics & medical · open membership
:::

:::
::: {.column width="51%"}

<div style="display:grid;">

::: {.fragment .current-visible style="grid-area:1/1; padding:0.4em 0.2em;" fragment-index="1"}
<div style="text-align:center; border:1px solid rgba(34,211,238,0.30); border-radius:9px; padding:0.45em 0.4em; margin-bottom:0.55em;">
<span style="font-size:1.85em; font-weight:800; color:#22d3ee; line-height:1.0;">€107 M</span><br>
<span style="font-size:0.62em; color:#94a3b8;">RAISE · Horizon Europe 2025–27<br>Resource for AI in Science in Europe</span><br>
<a href="https://research-and-innovation.ec.europa.eu/strategy/strategy-research-and-innovation/our-digital-future/european-ai-science-strategy/raise-resource-ai-science-europe_en" style="color:#67e8f9; font-size:0.56em;">ec.europa.eu → RAISE</a>
</div>
<div style="text-align:center; border:1px solid rgba(34,211,238,0.30); border-radius:9px; padding:0.45em 0.4em;">
<span style="font-size:1.85em; font-weight:800; color:#22d3ee; line-height:1.0;">$293 M</span><br>
<span style="font-size:0.62em; color:#94a3b8;">GENESIS · US DOE · 17 national labs · 2025</span><br>
<a href="https://www.energy.gov/undersecretaryforscience/genesis-mission/genesis-mission" style="color:#67e8f9; font-size:0.56em;">energy.gov → Genesis</a>
</div>
:::

::: {.fragment .current-visible style="grid-area:1/1; padding:0.4em 0.2em;" fragment-index="2"}
<div style="text-align:center; border:1px solid rgba(34,211,238,0.30); border-radius:9px; padding:0.45em 0.4em; margin-bottom:0.55em;">
<span style="font-size:1.5em; font-weight:800; color:#22d3ee; line-height:1.1;">RL4AA · Liverpool 2026</span><br>
<span style="font-size:0.62em; color:#94a3b8;">Tutorials · challenge · open datasets</span><br>
<a href="https://rl4aa.github.io/" style="color:#67e8f9; font-size:0.56em;">rl4aa.github.io</a>
</div>
<div style="text-align:center; border:1px solid rgba(34,211,238,0.30); border-radius:9px; padding:0.45em 0.4em;">
<span style="font-size:1.5em; font-weight:800; color:#22d3ee; line-height:1.1;">AISSAI · Oct 11–16 2026</span><br>
<span style="font-size:0.62em; color:#94a3b8;">AI for Particle Accelerators hackathon</span><br>
<a href="https://indico.in2p3.fr/event/39704/" style="color:#67e8f9; font-size:0.56em;">indico.in2p3.fr/event/39704</a> · <a href="https://aghribi1-presentation-20230911-artifact.docs.cern.ch/hackathon_Slidedeck#1" style="color:#67e8f9; font-size:0.56em;">slides</a>
</div>
:::

::: {.fragment .current-visible style="grid-area:1/1; padding:0.3em 0.2em;" fragment-index="3"}
<div style="border:2px solid #fbbf24; border-radius:10px; background:rgba(251,191,36,0.05); padding:0.5em 0.6em;">
<div style="font-size:1.35em; font-weight:800; color:#fbbf24; text-align:center; line-height:1.1;">
<a href="https://artifact-network.org" style="color:#fbbf24; text-decoration:none;">artifact-network.org</a></div>
<div style="font-size:0.58em; color:#cbd5e1; text-align:center; margin:0.3em 0 0.6em; line-height:1.35;"><strong style="color:#fbbf24;">ART</strong>ificial <strong style="color:#fbbf24;">I</strong>ntelligence <strong style="color:#fbbf24;">F</strong>or <strong style="color:#fbbf24;">A</strong>ccelerators, user <strong style="color:#fbbf24;">C</strong>ommunities and associated <strong style="color:#fbbf24;">T</strong>echnologies</div>
<div style="font-size:0.72em; color:#e2e8f0; line-height:1.65;">
<strong style="color:#fbbf24;">Shares</strong> data · beam-time · models · training material<br>
<strong style="color:#fbbf24;">Multi-project synergies</strong> —
<a href="https://artifact-network.org/initiatives/TwinRISE_Deck.html#1" style="color:#67e8f9;">TwinRISE</a> ·
<a href="https://www.irisproject.eu" style="color:#67e8f9;">IRIS</a> ·
<a href="https://www.sciance.eu" style="color:#67e8f9;">SCIANCE</a> ·
ARTIFACT Doctoral Networks
</div>
<div style="font-size:0.64em; color:#94a3b8; margin-top:0.55em; font-style:italic; border-top:1px solid rgba(251,191,36,0.25); padding-top:0.45em;">TwinRISE links AI across accelerators &amp; medical.</div>
</div>

<div style="font-size:0.40em; color:#64748b; margin-top:0.5em; line-height:1.45; text-align:center;">
This work has received funding from the European Union's Horizon Europe research &amp; innovation programme — grant agreements No. 101275935 (iRIS), 101287548 (TwinRISE) &amp; 101293570 (SCIANCE) — and from national &amp; EU partner programmes.
</div>
:::

</div>

:::
::::

::: notes
RAISE: the full EU AI in Science strategy aims to double Horizon Europe AI funding to €3B/year, with RAISE as the flagship virtual institute. The €107M is the initial Horizon Europe pilot budget (WP26-27). GENESIS: the $293M is the DOE Request for Applications; an additional $320M was announced for AI infrastructure (American Science Cloud). RL4AA 2027 is not yet announced.

Full funding acknowledgement (read out / on the printed version): This work has received funding from the European Union's Horizon Europe research and innovation programme under grant agreements No. 101275935 (iRIS), 101287548 (TwinRISE), 101293570 (SCIANCE), 101057511 (EURO-LABS), 101079773 (EuPRAXIA Preparatory Phase), 101004730 (I-FAST) and Marie Skłodowska-Curie 101086276 (EAJADE); from the Euratom Research and Training Programme under No. 101052200 (EUROfusion); from Spain's CDTI "Misiones" / NextGenerationEU under MIP-20221017 (DONES-FLUX), MIG-20221067 (NEURON-DONES), PLEC2024-011195 (OPTIMA-DONES) and ITC-20251204, and from MCIN/AEI under PID2021-123930OB-C22; from Italy's PNRR projects I-PHOQS (IR0000016), EuAPS (IR0000030) and THE (ECS00000017); from Xjenza Malta's FUSION R&I Programme (AICRYSCON); and from the French Development Agency / Expertise France (University of Nairobi mobility grant).
:::

---

## Summary

::: {style="font-size:0.82em; color:#cbd5e1; margin:0.4em 0 0.8em;"}
**AI is broader than deep learning.** We built three methods end to end — two neural, one classical (BO/GP) — on real beam-physics problems: **problem → method → result → runnable notebook.**
:::

::: {style="font-size:0.84em; line-height:1.75;"}
- **Three use cases, end to end** — fault detection (AUC ≈ 1) · beam tuning (BO + Cheetah, advantage grows with dimension) · surrogate (R² ≈ 1, ~600× faster).
- **Physics ↔ AI** — Hopfield, Boltzmann machines, the 2024 Nobel in Physics: we are natural practitioners, not outsiders.
- **The model is rarely the bottleneck** — data quality, features and the pipeline decide whether it works.
- **Deployment ≠ accuracy** — latency, drift, explainability and certification are the real blockers to the clinic.
:::

::: {style="text-align:center; font-size:0.72em; color:#22d3ee; margin-top:0.6em;"}
All code open & runnable on Colab — `github.com/aghribi/cas2026-ai-medical-accelerators`
:::

::: notes
A real summary, three layers. (1) The one-line thesis at top: AI is broader than deep learning, and we covered three methods — two neural, one classical — each taken end to end from problem to runnable notebook. (2) The table recaps each use case as problem → method → result; say "ROC-AUC 1.000 on *synthetic* data" out loud. (3) The three takeaway boxes are the lecture's real message beyond the demos: physics and AI are the same toolkit (Nobel 2024), the model is rarely the bottleneck (data and pipeline are), and deployment is gated by certification, not accuracy. Close on: everything is open and runnable on Colab. Slow down here — this is the last content slide before Q&A.
:::

---

## References & Further Reading

:::: {.columns style="margin-top:0.3em; gap:1.2em;"}
::: {.column width="58%" style="font-size:0.58em; line-height:1.7;"}

**Key papers — cited in this lecture**

Ghribi et al., *EPN* **56**(1), 15 (2025)<br>
Roussel et al., *PRAB* **27**, 084801 (2024)<br>
Kaiser et al., *PRAB* **27**, 054601 (2024)<br>
Duris et al., *PRL* **124**, 124801 (2020)<br>
Tennant et al., *PRAB* **23**, 114601 (2020)<br>
Hopfield, *PNAS* **79**, 2554 (1982)

::: {style="margin-top:0.6em;"}
**Living review —** <a href="https://aghribi.github.io/acc-ml-living-review" style="color:#22d3ee;">aghribi.github.io/acc-ml-living-review</a>
:::

:::
::: {.column width="40%" style="font-size:0.68em;"}

::: {.highlight-box style="padding:0.45em 0.7em; margin-bottom:0.35em;"}
**Notebooks on Colab**<br>
<a href="https://colab.research.google.com/github/aghribi/cas2026-ai-medical-accelerators/blob/main/notebooks/01_fault_detection/notebook.ipynb" style="color:#22d3ee;">01 · RF Fault Detection</a><br>
<a href="https://colab.research.google.com/github/aghribi/cas2026-ai-medical-accelerators/blob/main/notebooks/02_beam_tuning/notebook.ipynb" style="color:#22d3ee;">02 · Beam Tuning</a><br>
<a href="https://colab.research.google.com/github/aghribi/cas2026-ai-medical-accelerators/blob/main/notebooks/03_surrogate_model/notebook.ipynb" style="color:#22d3ee;">03 · Surrogate Model</a>
:::

::: {.highlight-box style="padding:0.45em 0.7em;"}
**RL4AA community**<br>
<a href="https://rl4aa.github.io/" style="color:#22d3ee;">rl4aa.github.io</a> — workshops · tutorials · challenge
:::

:::
::::

::: notes
Reference slide — leave it up during the transition to Q&A so people can photograph it. Rather than reading citations aloud, point them at two things: the living review, which is continuously updated and is the real map of the field, and the three Colab notebooks, which are the hands-on entry point. The six papers listed are the load-bearing ones if anyone asks for a single starting reference per topic.
:::

---

## Glossary — acronyms used in this lecture

:::: {.columns style="font-size:0.46em; line-height:1.55; margin-top:0.3em;"}
::: {.column width="33%"}

**AI** — Artificial Intelligence<br>
**ML** — Machine Learning<br>
**NN** — Neural Network<br>
**MLP** — Multi-Layer Perceptron<br>
**CNN** — Convolutional Neural Network<br>
**LSTM** — Long Short-Term Memory<br>
**AE** — Autoencoder<br>
**VAE** — Variational Autoencoder<br>
**RL** — Reinforcement Learning<br>
**PINN** — Physics-Informed Neural Network<br>
**SVM** — Support Vector Machine<br>

:::
::: {.column width="34%"}

**BO** — Bayesian Optimisation<br>
**GP** — Gaussian Process<br>
**EI** — Expected Improvement<br>
**LHS** — Latin Hypercube Sampling<br>
**PCA** — Principal Component Analysis<br>
**UQ** — Uncertainty Quantification<br>
**MC** — Monte Carlo (dropout)<br>
**ROC** — Receiver Operating Characteristic<br>
**AUC** — Area Under the ROC Curve<br>
**MSE / MAE** — Mean Squared / Absolute Error<br>
**R²** — Coefficient of Determination<br>

:::
::: {.column width="33%"}

**XAI** — Explainable AI<br>
**SHAP** — SHapley Additive exPlanations<br>
**LIME** — Local Interpretable Model-agnostic Explanations<br>
**FAIR** — Findable, Accessible, Interoperable, Reusable<br>
**ONNX** — Open Neural Network Exchange<br>
**IQ** — In-phase / Quadrature (RF)<br>
**SRF** — Superconducting Radio-Frequency<br>
**BPM** — Beam Position Monitor<br>
**EPICS · TANGO · DOOCS** — control systems<br>
**TRL** — Technology Readiness Level<br>

:::
::::

::: notes
Reference resource, not a slide to present — mention in one breath that acronyms are collected here, then move to the close. Accelerator-side terms (RF, BPM, SRF, LINAC, QA) are assumed known; the list focuses on the AI/ML vocabulary that a mixed audience may not share. Sourced from the proceedings' acronyms.tex.
:::

---

## {.plain background-color="#0d1b2a"}

::: {style="text-align: center; padding-top: 1em;"}

::: {style="font-size: 1.9em; font-weight: 700; color: #f1f5f9;"}
Thank you · Questions welcome
:::

::: {style="color: #94a3b8; font-size: 0.7em; margin-top: 0.4em;"}
Dr. Adnan Ghribi · GANIL / CNRS-IN2P3 · `adnan.ghribi@ganil.fr`
:::

:::

::: notes
Close and open the floor. Anticipated questions worth pre-loading an answer for: "Does AUC = 1.0 hold on a real machine?" — no, that was synthetic data; real signals are messier and that is precisely the open problem. "Why not RL for tuning?" — RL is excellent for sequential online control but overkill for single-shot optimisation, where BO is more sample-efficient. "How do you certify this for clinical use?" — that lands on the explainability and integration open problems and the EU AI Act high-risk classification. Thank the CERN Accelerator School and acknowledge GANIL / CNRS-IN2P3.
:::

# Backup {.section-divider background-color="#e2e8f0" data-background-transition="zoom"}

::: {style="color: #1e3a5f; font-size: 1.4em; font-weight: 800; margin-top: 0.5em;"}
Backup slides
:::

::: {style="font-size: 0.95em; color: #334155;"}
Deep-dives for questions — not part of the 50-minute talk
:::

::: notes
Everything past here is Q&A material only. Quick index: Hopfield capacity & retrieval · Minsky–Papert and the funding winter · BO internals (EI + kernels) · high-dimensional scaling (D=2/4/6) · autoencoder vs supervised/threshold · uncertainty quantification in numbers · RL worked example · EU AI Act & certification.
:::

---

## Hopfield — memory retrieval and capacity

:::: {.columns style="margin-top:0.3em; gap:1.5em;"}
::: {.column width="54%"}

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="1"}
**① Ball in a valley** — memories are energy minima.
Noisy input rolls downhill → arrives at nearest stored pattern.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em; margin-bottom:0.5em;" fragment-index="2"}
**② Spin update** — $s_i \leftarrow \text{sign}(\sum_j w_{ij} s_j)$
Energy decreases at every step: **guaranteed convergence**.
:::

::: {.fragment .fade-in-then-semi-out style="border-left:3px solid rgba(34,211,238,0.5); padding:0.35em 0.7em; font-size:0.76em;" fragment-index="3"}
**③ Capacity ≈ 0.14 N** — beyond this, retrieval fails **catastrophically** (sharp phase transition, not gradual).
:::

:::
::: {.column width="44%"}

<div style="display:grid;">

::: {.fragment .current-visible style="grid-area:1/1;" fragment-index="1"}

```{python}
#| echo: false
#| eval: true

import numpy as np
import plotly.graph_objects as go

x = np.linspace(-2.5, 2.5, 90)
y = np.linspace(-2.5, 2.5, 90)
X, Y = np.meshgrid(x, y)

E = (
    - 2.2 * np.exp(-2.2*((X - 1.3)**2 + (Y - 1.1)**2))
    - 2.2 * np.exp(-2.2*((X + 1.3)**2 + (Y + 1.1)**2))
    + 0.28*(X**2 + Y**2)
)

fig = go.Figure()
fig.add_trace(go.Contour(
    z=E, x=x, y=y,
    colorscale=[[0,"#0a2040"],[0.4,"#1a3a6f"],[0.75,"#22d3ee"],[1,"#f87171"]],
    contours=dict(showlabels=False, coloring='fill'),
    showscale=False, ncontours=22, opacity=0.9,
))
fig.add_trace(go.Scatter(
    x=[1.3, -1.3], y=[1.1, -1.1], mode="markers+text",
    marker=dict(color="#fbbf24", size=14, symbol="star"),
    text=["Memory 1", "Memory 2"],
    textposition=["top right", "bottom left"],
    textfont=dict(size=10, color="#fbbf24"),
    showlegend=False,
))
tx = [0.35, 0.60, 0.88, 1.10, 1.25, 1.30]
ty = [0.05, 0.30, 0.62, 0.88, 1.03, 1.10]
fig.add_trace(go.Scatter(
    x=tx, y=ty, mode="lines+markers",
    line=dict(color="#fb923c", width=2.5),
    marker=dict(color="#fb923c", size=7),
    showlegend=False,
))
fig.add_annotation(x=0.35, y=0.05, text="Noisy / partial input",
    font=dict(size=9, color="#fb923c"),
    showarrow=True, ax=-45, ay=28,
    arrowcolor="#fb923c", arrowwidth=1.5)
fig.add_annotation(x=0.88, y=0.62, text="each update:<br>energy ↓",
    font=dict(size=8.5, color="#fb923c"), showarrow=False,
    xanchor="left", bgcolor="rgba(13,27,42,0.7)")
fig.add_annotation(x=1.3, y=1.1, text="recalled<br>memory",
    font=dict(size=9, color="#fbbf24"),
    showarrow=True, ax=38, ay=-28,
    arrowcolor="#fbbf24", arrowwidth=1.5)
fig.add_annotation(x=-1.3, y=-1.1, text="energy<br>minimum",
    font=dict(size=8.5, color="#94a3b8"),
    showarrow=True, ax=-38, ay=22,
    arrowcolor="#475569", arrowwidth=1.2)
fig.update_layout(
    height=270, autosize=True,
    paper_bgcolor="#0d1b2a", plot_bgcolor="#0d1b2a",
    xaxis=dict(showgrid=False, showticklabels=False, zeroline=False),
    yaxis=dict(showgrid=False, showticklabels=False, zeroline=False),
    margin=dict(l=5, r=5, t=28, b=5),
    font=dict(color="#e2e8f0"),
    title=dict(text="Network state space — stored memories = energy minima",
               font=dict(size=10, color="#94a3b8"), x=0.5, y=0.99),
)
fig
```

:::

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.5em 0.5em;" fragment-index="2"}
<div style="font-size:1.1em; color:#22d3ee; font-weight:700; margin-bottom:0.5em; font-family:monospace;">
s<sub>i</sub> ← sign(Σ w<sub>ij</sub> s<sub>j</sub>)
</div>
<div style="font-size:0.72em; color:#94a3b8; line-height:1.6;">
Hebb rule · ΔE ≤ 0 per step<br>
<span style="color:#fbbf24;">Same physics as spin relaxation</span>
</div>
:::

::: {.fragment .current-visible style="grid-area:1/1; text-align:center; padding:1.8em 0;" fragment-index="3"}
<span style="font-size:2.4em; font-weight:800; color:#22d3ee; line-height:1.0;">0.14 N</span><br>
<span style="font-size:0.72em; color:#94a3b8; display:block; margin-top:0.4em;">Capacity limit · recall error: 0 → 50%</span>
:::

</div>

:::
::::

::: {.slide-ref}
Amit, Gutfreund & Sompolinsky (1985). Storing infinite numbers of patterns in a spin-glass model. *PRL* 55, 1530.
:::

::: notes
**① The contour plot — energy landscape:** the axes are abstract coordinates in the space of all possible network states; each point is one configuration of all N neurons. Colour = energy. The two yellow stars are stored memories (energy minima); the orange trajectory is a noisy input rolling downhill to the nearest one. Particle-in-a-potential dynamics, where the "particle" is the whole network state.

**② Spin update s_i ← sign(Σ_j w_{ij} s_j):** one can prove ΔE ≤ 0 at every update (E = −½ Σ w_{ij} s_i s_j, w symmetric), so convergence to a local minimum is guaranteed — identical to spin relaxation in an Ising magnet at zero temperature.

**③ Why 0.14N:** with load α = P/N, the other P−1 patterns inject cross-talk noise ~√(P/N). Amit–Gutfreund–Sompolinsky (1985) computed the critical α_c ≈ 0.138 with the replica method (spin-glass theory) — a genuine field-theoretic result, not a scaling estimate. The transition is first-order: below it recall error ≈ 0, at it the error jumps to ~0.5. A 1000-neuron network stores ~138 patterns, then breaks catastrophically.
:::

---

## How Minsky & Papert froze the field (1969) {.smaller}

::: {.col-left}

**1969 — *Perceptrons*** (Minsky & Papert) proved a single-layer perceptron **cannot compute XOR**, nor any non-linearly-separable function.

Impact as much *sociological* as mathematical:

- US funding agencies read it as "neural nets are a dead end"
- Connectionist funding collapsed through the 1970s
- The field pivoted to **symbolic AI** / expert systems
- Revived only by **backprop** (Werbos 1974; Rumelhart–Hinton–Williams 1986) — which trains the hidden layers they dismissed

:::
::: {.col-right}

::: {.highlight-box}
**The irony**

XOR *is* solvable — with **one hidden layer**. The authors knew it, but wrongly argued such nets could never be trained at scale. Backprop proved them wrong 17 years later.
:::

::: {.key-result style="margin-top:0.6em;"}
One influential "impossibility" result can freeze a direction for a decade. Scepticism cuts both ways.
:::

:::

::: {.clearfix}
:::

::: {.slide-ref}
Minsky & Papert (1969). *Perceptrons*, MIT Press. · Werbos (1974) · Rumelhart, Hinton & Williams (1986), *Nature* 323, 533.
:::

::: notes
Backup for the AI-winters slide. The take-home is the sociology of science, not just the XOR maths: a single authoritative result, amplified by funding decisions, set the field back a decade even though the technical fix (hidden layers) was already conceivable. Good answer if someone asks "why did it take so long?"
:::

---

## Backup — Bayesian optimisation internals {.smaller}

::: {.col-left}

**Expected Improvement** acquisition function:

$$\alpha_{\text{EI}}(x) = \mathbb{E}\big[\max(0,\; f_{\text{best}} - f(x))\big]$$

For a GP posterior with mean $\mu(x)$, std $\sigma(x)$, and $z = \dfrac{f_{\text{best}} - \mu(x)}{\sigma(x)}$:

$$\alpha_{\text{EI}}(x) = \sigma(x)\,\big[\,z\,\Phi(z) + \phi(z)\,\big]$$

- $\Phi, \phi$ — standard-normal CDF / PDF
- term 1 **exploits** (low predicted mean), term 2 **explores** (high variance)
- next probe = $\arg\max_x \alpha_{\text{EI}}(x)$

:::
::: {.col-right}

::: {.highlight-box style="font-size:0.82em;"}
**The kernel is the prior on smoothness**

- **Matérn 5/2** — twice-differentiable; default for physical systems
- **RBF** — infinitely smooth (often *too* smooth for real data)
- **Periodic** — cyclic signals (RF phase)
:::

::: {.key-result style="margin-top:0.6em; font-size:0.82em;"}
Kernel choice encodes physics — the single most consequential modelling decision in BO.
:::

:::

::: {.clearfix}
:::

::: notes
Backup for UC2. If asked "how does BO actually pick the next point": EI balances exploit (first term) and explore (second term) in closed form from the GP posterior. The kernel question is the one experts ask — Matérn 5/2 is the sane default because real beam responses are continuous but not infinitely smooth; RBF over-smooths and can miss sharp features.
:::

---

## Backup — BO vs grid / random as dimension grows

::: {style="font-size:0.8em; margin-top:0.2em;"}
Controlled experiment (notebook 02 §7b): a *D*-quad matching section, **the same budget B = 120 evaluations** for every method, log(σ_x+σ_y) objective, 4% noise, 3 seeds.
:::

::: {style="font-size:0.92em;"}
| Dimension | Grid | Random | **Bayesian opt.** |
|:--|:--:|:--:|:--:|
| **D = 2** (2 quads) | 5.1 mm | 4.7 mm | **3.4 mm** |
| **D = 4** (4 quads) | 7.2 mm | 3.0 mm | **1.2 mm** |
| **D = 6** (6 quads) | 5.9 mm | 4.6 mm | **1.5 mm** |
:::

::: {.key-result style="margin-top:0.5em; font-size:0.84em;"}
At D = 6, BO is ~3× better than either baseline and **every seed wins**. A full-resolution 7ᴰ grid would need 7⁶ = **117,649** evaluations; BO used 120.
:::

::: {style="font-size:0.74em; color:#94a3b8; margin-top:0.4em; line-height:1.5;"}
Why grid/random collapse: usable volume shrinks as ~rᴰ, so a fixed budget covers exponentially less of the space. BO builds a surrogate and spends evaluations where they matter. And note D = 4/6 reach a *smaller* spot than D = 2 — more quads give more focusing freedom, which BO exploits and classical methods cannot.
:::

::: notes
This is the slide that backs the "BO wins as dimension grows" claim with measured numbers, not extrapolation. The 2D result is honestly a near-tie; the story only becomes decisive at D = 4 and beyond. Pull this up if anyone challenges why BO over a simple grid.
:::

---

## Backup — why an autoencoder, not a classifier?

::: {style="font-size:0.82em;"}
| Approach | Needs fault labels? | Detects *unseen* fault types? | Fit for clinical RF |
|:--|:--:|:--:|:--|
| **Threshold / interlock** | no | no — only hard-coded limits | safety net, not detection |
| **Supervised classifier** (Tennant 2020) | **yes — many per class** | no | great *when* labels exist |
| **Autoencoder (ours)** | **no — normal data only** | **yes** | fits label-scarce clinical ops |
:::

::: {.highlight-box style="margin-top:0.5em; font-size:0.84em;"}
The autoencoder learns "normal"; anything it reconstructs poorly is flagged — including fault modes never seen in training. A supervised classifier can only recognise the classes it was trained on.
:::

::: {.key-result style="margin-top:0.5em; font-size:0.82em;"}
Trade-off: the autoencoder tells you *something is wrong*, not *what*. Pair it with a classifier once labels accumulate.
:::

::: notes
Backup for UC1, and the honest framing of the Tennant-2020 comparison: the CEBAF system is a supervised classifier on labelled data — the right tool there. Our autoencoder is the right tool when no labels exist, which is the clinical reality. They are complementary, not competing.
:::

---

## Backup — uncertainty quantification, in numbers

::: {.col-left}

Two ways to make the MLP surrogate report confidence (notebook 03):

**Deep ensemble** — train N models from different initialisations; the spread of their predictions is the uncertainty.

**MC-dropout** — keep dropout active at inference and run K stochastic forward passes.

:::
::: {.col-right}

::: {.highlight-box style="font-size:0.84em;"}
**Measured — MC-dropout, σ_x+σ_y**

- R² ≈ 0.999
- σ_MC ≈ 0.13 mm
- ~99.7% empirical coverage (within ±3σ)
:::

:::

::: {.clearfix}
:::

::: {.key-result style="margin-top:0.5em; font-size:0.82em;"}
**Epistemic** (reducible by more data) vs **aleatoric** (irreducible noise). For control you want *calibrated* σ — the error bars should match the actual errors.
:::

::: notes
Backup for UC3. The key word is "calibrated": a model that reports σ = 0.13 mm but is routinely wrong by 1 mm is worse than useless in a control loop. Coverage near the nominal 99.7% (±3σ) is the check that the uncertainty is honest.
:::

---

## Backup — reinforcement learning in the control loop

::: {.col-left}

**Setup:** the agent observes machine state $s$ (BPMs, losses), takes action $a$ (corrector / quad set-points), receives reward $r$ (e.g. −beam loss), and learns a policy $\pi(a\,|\,s)$ that maximises cumulative reward.

**Why RL, not BO, here?** RL *amortises*: it learns a reusable policy for a recurring sequential task, where BO re-optimises from scratch every time.

:::
::: {.col-right}

::: {.highlight-box style="font-size:0.82em;"}
**Demonstrated**

- **CERN** — sample-efficient RL for steering (Kain et al. 2020)
- **DESY** — online RL + BO injector tuning (Kaiser et al. 2023)
- **RL4AA** — community challenge & open benchmarks
:::

::: {.key-result style="margin-top:0.6em; font-size:0.8em;"}
Open problems on a clinical machine: sample efficiency and *safe* exploration.
:::

:::

::: {.clearfix}
:::

::: {.slide-ref}
Kain et al., *PRAB* 23, 124801 (2020) · Hirländer et al. (2020) · Kaiser et al. (2023)
:::

::: notes
Backup for the futures slide, since RL is named but never shown. The crisp distinction to offer: BO is single-shot optimisation, RL learns a policy for a repeated sequential task. On a clinical machine the blocker is not capability but safe exploration — you cannot let an agent try arbitrary set-points on a live patient beam.
:::

---

## Backup — EU AI Act & clinical certification

::: {.col-left}

The **EU AI Act (2024)** is risk-tiered. AI **in the control loop of a medical device** falls in the **high-risk** class, which requires:

- documented training data & provenance
- demonstrable robustness and accuracy
- meaningful human oversight
- **explainable, auditable** decisions

:::
::: {.col-right}

::: {.highlight-box style="font-size:0.82em;"}
**Practical consequences**

- black-box models need an XAI layer (SHAP / LIME) for audit
- versioned, reproducible models (FAIR + ONNX)
- a certification pipeline that re-validates after every retrain
:::

::: {.key-result style="margin-top:0.6em; font-size:0.8em;"}
This is why "AUC = 1.0 in a notebook" is the *start*, not the end. Certification, not accuracy, is the bottleneck to clinical deployment.
:::

:::

::: {.clearfix}
:::

::: notes
Backup for the open-problems and certification questions. The point that resonates with this audience: the hard part of clinical AI is not the model — it is proving, to a regulator, that the model is safe, reproducible, and auditable after every retrain. That reframes the whole lecture's "open problems" as regulatory-engineering problems, not ML ones.
:::

